# MonprojetPro — Patch Log

> Maintenu par ATLAS (Patch Mode) — 1 ligne par correction/patch/fix appliqué en session.
> Colonne "Capitalisé" : Oui si la leçon a été enregistrée dans `08-lessons-learned.md`, Non si fix trivial.

| Date | Fix/Patch | Fichiers | Leçon | Capitalisé |
|------|-----------|----------|-------|------------|
| 2026-07-11 | **Alertes système fantômes — auto-résolution + panne durable.** Les alertes cloche du health-check-cron restaient affichées même après le rétablissement du service (« monitoring vert mais alerte présente », retour MiKL). Refonte : suivi d'incidents par service (`system_config.health_incidents` : `errorSince` + `notificationId`) → (1) alerte seulement si l'erreur DURE ≥ 15 min (`SUSTAINED_ERROR_MS`, remplace la confirmation 2 cycles + debounce 60 min de `getAlertingServices`/`shouldSendAlert`, supprimés) ; (2) **auto-résolution** : au retour ok/degraded, l'alerte cloche ouverte est SUPPRIMÉE. Hook Realtime étendu à UPDATE/DELETE + migration `notifications REPLICA IDENTITY FULL` (sinon les DELETE filtrés par `recipient_id` ne sont pas délivrés au client). 2 fantômes legacy purgés. Edge fn déployée (v11, verify_jwt préservé), invocation test → 200 `ok`, `health_incidents={}`. 22 tests logique verts. Couche externe (Better Stack, push téléphone) proposée par SCOUT, à faire ensuite. | supabase/functions/health-check-cron/{index.ts, health-check-logic.ts, health-check-logic.test.ts}, supabase/migrations/20260711100000_notifications_replica_identity_full.sql, packages/modules/notifications/hooks/use-notifications-realtime.ts | Oui → lessons-learned |
| 2026-07-07 | **Parcours graduation One — boucle « Félicitations » + thème violet + textes périmés (retour MiKL, captures).** ① « Accéder au dashboard » rebouclait sur l'animation : `markGraduationScreenShown` faisait un UPDATE `clients` filtré EN SILENCE par la RLS (aucune policy UPDATE owner, 0 ligne, 0 erreur) → flag jamais posé → middleware re-redirige vers celebrate. Fix : RPC `fn_mark_graduation_screen_shown` SECURITY DEFINER (migration 00143) + vérif row count + l'UI destructure `{ error }` et ne redirige plus en cas d'échec. ② Tour 4 étapes en violet Lab : `/graduation/*` hors groupe (dashboard) → `theme-lab` du layout racine jamais corrigé (le `data-theme='one'` posé par celebrate ne pilote rien) → `ThemeOneSetter` (classe `theme-one` + transition) dans le layout graduation. ③ Textes vision v1 périmés + module CRM fantôme dans le tour et discover-one → réécriture vision One v2 (console de pilotage + lien MiKL), catalogue aligné sur `ALL_CLIENT_MANIFESTS` (suivi-outil, documents, elio, chat, visio, support), étape toggle Lab/One conditionnée à `lab_mode_available`, gradient orange retiré. | `supabase/migrations/00143_fn_mark_graduation_screen_shown.sql`, `packages/types/src/database.types.ts`, `apps/client/app/graduation/{actions/mark-graduation-screen-shown.ts(+test),layout.tsx,theme-one-setter.tsx,tour-one/page.tsx,discover-one/page.tsx}`, `apps/client/app/components/graduation/{graduation-tour,graduation-tour-skip,graduation-celebrate}.tsx`, `apps/client/app/hooks/use-graduation-tour.ts` | DB-002 : UPDATE filtré par RLS = 0 ligne SANS erreur → toujours vérifier l'effet (row count), jamais conclure au succès sur l'absence d'erreur. Même bug latent à vérifier dans `complete-onboarding.ts`. | Oui → lessons-learned |
| 2026-06-29 | **Élio One — cohérence + continuité (retour MiKL, 3 itérations).** ① L'onglet sidebar Élio ouvrait un chat plein écran avec historique en violet (couleurs Lab) au lieu de la pop-up éphémère verte de l'accueil → refonte « pop-up UNIQUE partout » : `/modules/elio` redirige, pop-up globale (`ElioOnePopup`) montée dans le layout, ouverte de partout via événement `openElioOnePopup()` (pas de zustand, non déclaré). ② Violet persistant dans la pop-up (portail Radix hors arbre `--brand-accent` + `--primary` orange du thème One) → override `--primary`/`--primary-foreground` + ring vert littéral sur `ElioChatSimple` One. ③ Widget sidebar et pop-up = 2 conversations séparées → « Voir dans Élio » s'ouvrait vide → **session partagée** (`ElioOneSessionProvider` = un seul `useElioChat` au layout, prop `externalSession` sur `ElioChat`) ; widget simplifié (suppression 3 modes). Demande d'évolution rebranchée en bouton dans la pop-up. Header chantier : bande oblique unique + cône à gauche du nom. | `apps/client/components/{elio-one-session,elio-one-popup,use-elio-one-popup,one-elio-box,one-home,one-activity-cockpit,one-cockpit-card}.tsx`, `apps/client/app/(dashboard)/{layout,page,modules/elio/page}.tsx`, `packages/modules/elio/{components/elio-chat.tsx,index.ts}`, `packages/ui/src/globals.css` | Pop-up/portail Radix = hors arbre des CSS vars de thème → forcer les couleurs en littéral/fallback. + widget & pop-up qui doivent partager une conversation = UNE session liftée via contexte, pas deux `useElioChat`. Voir [[elio-one-popup-unique]]. | Non (capitalisé en mémoire projet) |
| 2026-06-16 | **« Je désactive le Lab côté Hub mais le client reste en Lab » → refonte complète du modèle Lab/One (vision produit / ADR-01).** D'abord une chaîne de consumers oubliés/divergents : (1) `toggleAccess` ne remettait pas `lab_mode_available=false` à la désactivation → le client gardait sa vue Lab via son cookie ; (2) `client_configs` **absente de la publication Realtime** → aucune propagation live côté client ; (3) la home (`page.tsx`) **et** la page `/modules/parcours` résolvaient le mode **sans** le garde `lab_mode_available` (contrairement au layout) → One-shell + contenu Lab, puis page parcours non gardée. Puis **build du modèle complet conforme à la matrice d'accès** (les 2 modes apparaissent ; clic sur mode verrouillé = message ; gradué = Lab historique ou entier) : flag **`one_mode_available`** (prévu par l'ADR-01 mais jamais codé), **résolveur de mode centralisé `resolveClientMode`** (fin des 4 copies divergentes), toggle intelligent (cadenas + dialog teasing verbatim vision produit), accès Lab gradué (`LabHistoryView` lecture seule si Élio Lab off, parcours entier sinon). | migrations `20260616171500_add_client_configs_to_realtime`, `20260616180000_add_one_mode_available` ; `packages/utils/src/client-mode.ts(+test)` ; `packages/ui/src/components/mode-toggle{,-constants}.tsx(+test)` ; `apps/client/app/(dashboard)/{layout,page,components/realtime-dashboard-refresh,modules/parcours/page,modules/elio/page}.tsx` ; `packages/modules/crm/actions/{toggle-access,graduate-client,create-client}.ts` ; `packages/types/src/{client-config,database}.types.ts` | DRY-001 : une logique de décision (résolution de mode) dupliquée dans N consumers diverge et casse en cascade → un résolveur unique partagé. + consumer Realtime oublié (`client_configs`). + un flag d'ADR jamais implémenté = dette silencieuse qui force le code à bricoler. | Oui → lessons-learned |
| 2026-06-16 | **Header client (Hub) en lecture seule + indicateurs Lab/One (retour MiKL).** Les toggles Lab/One du header dupliquaient l'action de l'onglet Lab ① (mais sans son garde-fou de confirmation) et le toggle « One » était inopérant pour un client Lab (One implicite). Header passé en **indicateurs lecture seule** (pastilles `Lab` violet / `One` vert, code couleur thème, grisé si inactif) ; l'activation/désactivation vit uniquement dans l'onglet Lab ①. | `packages/modules/crm/components/client-header.tsx` | — | Non (itération UX) |
| 2026-06-16 | **Refonte onglet Lab (audit ergonomie, retour MiKL).** Onglet « mal fichu » : 3 blocs empilés sans ordre, activation tout en bas, confusion « parcours Lab actif » vs « processus Lab activé ». **Découverte** : `AccessToggles` (→ `toggleAccess`, activer/désactiver le Lab SANS paiement, journalisé) existait + testé mais **n'était branché nulle part** → activation manuelle impossible côté UI. Refonte en 3 étapes (① Activation → ② Accompagnement → ③ Évolution), en-tête d'état unique (« Lab activé/non » + comment : payé / manuel / en attente), activation enfin surfacée, facturation + config Élio passés en slots (frontières modules). Fix : `toggleAccess` active aussi `elio_lab_enabled` + `lab_mode_available` (comme le paiement). | `packages/modules/crm/{components/client-lab-tab-content.tsx(+test), actions/toggle-access.ts}`, `apps/hub/app/(dashboard)/modules/crm/clients/[clientId]/client-detail-with-support.tsx` | UI-004 : un composant codé+testé mais jamais rendu = capacité invisible (coquille inverse). + activation manuelle doit refléter TOUS les flags du chemin automatique (elio_lab_enabled). | Oui → lessons-learned |
| 2026-06-16 | **Interface figée — sélection de texte sur le chrome (retour MiKL).** Le curseur I-beam + caret apparaissait au clic sur tout texte des dashboards (effet "document"). Choix MiKL : geler la sélection sur le chrome (titres, labels, en-têtes, boutons, liens, nav/sidebar, menus) en gardant les données (p, td, spans) et le contenu de document (.prose) copiables. Règle `user-select` ajoutée au `globals.css` partagé (Hub + Lab/One). | `packages/ui/src/globals.css` | — | Non (polish UX) |
| 2026-06-16 | **Menu ⋯ non cliquable dans le dialogue CRM (retour MiKL).** Le menu (porté sur `<body>` pour échapper à l'`overflow`) tombait hors du contenu du dialogue Radix **modal** « Gestion des documents », qui met `pointer-events:none` sur tout l'extérieur → éléments visibles mais non cliquables. Fix : `pointerEvents:'auto'` sur le menu porté. | `packages/modules/documents/components/document-list.tsx` | UI-003 (complément) : un élément porté hors d'un dialogue Radix modal doit forcer `pointer-events:auto`. | Oui → lessons-learned |
| 2026-06-16 | **Sync Realtime du partage côté Lab + fuite RLS inter-clients (retour MiKL).** Retirer le partage d'un doc ne se synchronisait jamais sur le Lab (refresh obligatoire). Cause : la RLS `documents_select_merged` ne montre au client que `visibility='shared'` → au passage en `private` le client perd l'accès SELECT, donc `postgres_changes` ne lui livre PAS l'event (Realtime applique la RLS sur la ligne livrée). Fix : **broadcast depuis la base** (`realtime.send` via trigger `trg_broadcast_documents_change` → canal `documents:<clientId>`, contourne la visibilité de ligne ; résilient pour ne jamais bloquer un upload) + hook `useDocumentsRealtime` qui invalide le cache. **Sécurité (CERBÈRE 🔴)** trouvée au passage : la même policy autorisait `visibility='shared'` **sans vérifier le propriétaire** → lecture inter-clients via l'API directe ; branche `shared` restreinte au client courant. | `supabase/migrations/20260616093000_documents_realtime_broadcast.sql`, `supabase/migrations/20260616093500_documents_select_fix_cross_client_leak.sql`, `packages/modules/documents/{hooks/use-documents-realtime.ts(+test), components/documents-page-client.tsx, index.ts}` | RT-001 : Realtime postgres_changes ne livre PAS un changement qui fait SORTIR la ligne de la visibilité RLS (unshare) → broadcast DB. SEC : RLS `shared` sans contrôle propriétaire = fuite inter-locataires. | Oui → lessons-learned |
| 2026-06-16 | **UX menu actions liste documents (Hub + Lab).** Menu ⋯ tronqué (position absolute rognée par l'`overflow-auto` de la DataTable) → rendu en PORTAL (position fixed, ouverture vers le haut si peu de place, fermeture au scroll/resize). Ajout de l'action **Télécharger** (absente de ce menu alors que dispo ailleurs). | `packages/modules/documents/components/document-list.tsx(+test)` | UI-003 : un menu en position absolute dans un conteneur `overflow-auto` est rogné → portal + position fixed. | Oui → lessons-learned |
| 2026-06-16 | **UX liste documents — dossiers en barre horizontale (retour MiKL).** La colonne de dossiers à gauche (~256px) écrasait le tableau et forçait le scroll latéral. Déplacée en barre de pilules horizontale au-dessus, liste pleine largeur en dessous. `FolderTree` + `FolderTreeSkeleton` reçoivent un prop `layout` ('sidebar' défaut / 'horizontal'). Fonctions dossiers préservées (créer/renommer/supprimer/sélection). | `packages/modules/documents/components/{folder-tree.tsx, folder-tree-skeleton.tsx, documents-page-client.tsx}` | — | Non (itération UX) |
| 2026-06-16 | **Lecture des documents impossible dans le dialogue « Gestion des documents » (CRM) + UX liste.** Cause racine : `client-documents-tab.tsx` appelait `DocumentsPageClient` **sans `viewerBaseHref`** → dans `DocumentList`, le nom rendu en `<span>` mort (pas de lien viewer). Fix : aperçu EN PLACE (`DocumentPreviewModal`, réutilise `useDocumentViewer` + `markdownToHtml` + `DocumentViewer`) déclenché par un bouton « Lire » (œil) sur chaque ligne + nom cliquable partout (lien si `viewerBaseHref`, sinon aperçu). UX : actions Déplacer/Supprimer regroupées dans un menu `⋯` (fin du scroll latéral), partage en icône compacte, colonnes Tags/Taille/Date/Sync masquées en responsive. Bonus : header « tout sélectionner » passé de fonction → JSX (un header-fonction ne s'affichait pas dans `DataTable`). | `packages/modules/documents/components/{document-preview-modal.tsx(+test), document-list.tsx(+test), document-share-button.tsx(+test)}` | Consumer oublié (même classe RGPD/Realtime) : un composant partagé qui dépend d'un prop optionnel (`viewerBaseHref`) devient une coquille morte chez le consumer qui l'oublie ; + header-fonction non rendu par `DataTable` (rend `{column.header}` brut). | Oui → lessons-learned |
| 2026-06-11 | **Système RGPD (export données client) rendu fonctionnel de bout en bout + chaîne email transactionnelle activée.** (1) **Export RGPD = coquille vide** : le bouton créait une row `data_exports` en `pending` éternel car les Edge Functions n'étaient **pas déployées** (seules `elio-chat` + `detect-overdue-invoices` l'étaient) → déploiement `generate-client-export` + `cleanup-expired-exports` (MCP). (2) **Lien download** : pointait vers le Hub (401 client) → chemin **relatif** + création de la route `/api/exports/[id]/download` côté app cliente. (3) **Emails transactionnels jamais envoyés** (0 log `email_sent`) : `send-email` non déployée + `pg_net` absent + trigger désactivé (`app.settings` NULL) → déploiement `send-email` (version consolidée auto-contenue), `CREATE EXTENSION pg_net`/`pg_cron`, trigger réécrit self-contained (URL+anon en dur), cron nettoyage quotidien. Clé Resend posée par MiKL → test envoi réel OK. (4) **Lien email** vers `lab.monprojet-pro.com` (NXDOMAIN) → URLs Vercel réelles configurables via `CLIENT_APP_URL`/`HUB_URL`. (5) **Anti-XSS** bouton CTA email (schéma `https?://` + échappement). (6) **Email login désynchronisé** : changer l'email d'une fiche client (Hub) ne mettait pas à jour `auth.users` → `updateClient` sync via `auth.admin.updateUserById`. (7) **Login figé** quand `redirectTo` = route API (download) → atterrissage dashboard + download en arrière-plan + anti open-redirect. | `supabase/functions/{generate-client-export,cleanup-expired-exports,send-email}/index.ts`, `supabase/functions/_shared/email-templates/{export-ready,base}.ts`, `supabase/migrations/20260610*_{enable_pg_net,selfcontained_email_trigger,schedule_cleanup}*.sql`, `apps/client/app/api/exports/[exportId]/download/route.ts`, `apps/client/app/(auth)/login/login-form.tsx`, `packages/modules/crm/actions/update-client.ts(+test)` | Edge Functions sur disque ≠ déployées (coquille vide) ; trigger email OFF si `app.settings` NULL + pg_net absent ; email client = 2 sources (auth.users **et** clients) à synchroniser ; lien email doit viser le vrai domaine (sous-domaines non branchés en DNS) | Oui → mémoire (edge-functions-non-deployees, email-resend-pending, email-deux-sources-auth-clients) |
| 2026-06-06 | **Consentement IA réellement bloquant + transparence + re-consentement.** Le refus du traitement IA était une coquille vide (tracé en base mais Élio continuait d'appeler Claude). Verrou serveur universel dans `sendToElio`/`newConversation` (chat libre + chat d'étape parcours), état « Élio en veille » sur les 3 surfaces. Politique IA corrigée (annonçait **DeepSeek** alors que le code appelle **Claude/Anthropic/US** + transfert hors UE). Historique des consentements (audit). Mécanisme de re-consentement IA. **Bug de suivi** : la 1ʳᵉ version redirigeait via le middleware → « unexpected response » + utilisateur piégé en **navigation interne** (soft-nav RSC) ; corrigé en déplaçant le déclenchement dans le layout serveur avec `redirect()`. | `packages/modules/elio/actions/{send-to-elio,new-conversation}.ts`, `apps/client/components/elio-veille.tsx`, `apps/client/components/one-elio-box.tsx`, `apps/client/app/(dashboard)/layout.tsx`, `apps/client/app/(auth)/ia-consent-update/{page,actions}.tsx`, `apps/client/app/(auth)/legal/ia-processing/page.tsx`, `apps/client/app/(dashboard)/settings/consents/*`, `apps/client/middleware*.ts`, `packages/modules/parcours/components/{parcours-step-detail,step-elio-chat}.tsx`, `packages/supabase/src/queries/get-consent.ts` | RSC-007 : redirection middleware KO en nav interne (soft-nav RSC) → `redirect()` serveur pour les interstitiels forcés. + Coquille vide RGPD : un consentement tracé doit être branché sur tous ses consumers. | Oui → lessons-learned |
| 2026-06-05 | **Itérations UX module support (retours MiKL).** (1) Boutons de statut déplacés dans la colonne droite sous la date. (2) Description repliable « Voir plus / Voir moins » (composant `ExpandableText`, mesure réelle du débordement), Hub + client. (3) **Textarea débordait toute la modale** sur texte long sans espaces → `field-sizing-content` + `min-width:auto` qui prime sur `max-width` ; fix `min-w-0/max-w-full/break-words` sur le `Textarea` de base. (4) Bouton statut actif : texte blanc lisible (n'est plus `disabled` → plus d'`opacity-50` grisée). | `packages/ui/src/textarea.tsx`, `packages/modules/support/components/{client-support-tab,my-tickets-list,expandable-text,report-issue-dialog}.tsx` | UI-001 : `field-sizing-content` + `min-width:auto` fait déborder un textarea hors de son conteneur (min-width prime sur max-width) | Oui → lessons-learned |
| 2026-06-05 | **Déploiement fantôme du Hub — deps Turbo non déclarées.** Les boutons de statut (commit `9547c4d`) ne s'affichaient jamais sur le Hub malgré Vercel « success ». Cause : `apps/hub/package.json` importait `modules-support` (+ 6 autres modules) sans les déclarer → Turbo n'invalidait pas le cache du Hub → ancien build resservi. Fix : déclaration des 7 modules manquants + `npm install` → vrai rebuild. La FAQ marchait car l'app client, elle, déclarait `modules-support`. | `apps/hub/package.json`, `package-lock.json` | DEP-006 : build Vercel « success » ≠ code récent servi (cache Turbo) ; déclarer tout module importé | Oui → lessons-learned |
| 2026-06-05 | **Module support — cycle de vie ticket complété.** (1) Hub : menu déroulant de statut remplacé par 4 boutons directs colorés. (2) **Bug "le changement de statut n'apparaît pas côté client"** : cause racine = `support_tickets` absente de la publication Realtime → migration 00141 `ALTER PUBLICATION supabase_realtime ADD TABLE` + hook `useSupportTicketsRealtime` monté dans `MyTicketsList`. (3) Notification client à chaque changement de statut (message adapté par état, insert direct dans `notifications` sans import cross-module). (4) Badge statut coloré par état (rouge/ambre/vert/gris, partagé Hub+client). (5) Refonte visuelle FAQ (icônes catégories, accents thème, accordéon vivant). Migration 00135 (bucket screenshots, oubliée pendant la migration Supabase) aussi déployée en amont. | `supabase/migrations/00141_*.sql`, `packages/modules/support/{actions/update-ticket-status.ts, components/{client-support-tab,ticket-status-badge,faq-page,my-tickets-list}.tsx, hooks/use-support-tickets-realtime.ts, index.ts}` | Consumer Realtime oublié (même classe que step_submissions/00121) : toute table lue côté client doit être dans `supabase_realtime` | Oui → lessons-learned |
| 2026-05-11 | Fix "Service IA indisponible" génération document parcours Lab — `generateDocumentFromConversation` et `submitGeneratedDocument` interrogeaient `parcours_steps` alors que le nouveau système utilise `client_parcours_agents`. Migration 00116 : suppression FK contraintes + ajout type `step_submission`. `ANTHROPIC_API_KEY` ajoutée dans `apps/client/.env.local`. | `packages/modules/parcours/actions/generate-and-submit-step.ts`, `submit-generated-document.ts`, `supabase/migrations/00116_*.sql` | — | Non (architecture shift, pas un pattern nouveau) |
| 2026-04-23 | **Toggle Mode Lab/One cassé — `MODE_TOGGLE_COOKIE = undefined` côté RSC** : Le cookie `mpp_active_view` était bien posé (visible DevTools + middleware) mais le layout serveur retournait toujours `activeMode='lab'`. Cause racine : `MODE_TOGGLE_COOKIE` était ré-exporté depuis `mode-toggle.tsx` (`'use client'`) via `packages/ui/src/index.ts`. Next.js 15 transforme les constantes de fichiers client en références client → côté RSC la valeur est `undefined`. Donc le layout faisait `cookieStore.get(undefined)` → `undefined` toujours. Fix : déplacer `MODE_TOGGLE_COOKIE` dans `mode-toggle-constants.ts` (sans directive), exporter depuis là dans `index.ts`. Bonus : création `mode-toggle-action.ts` avec `'use server'` pour poser le cookie côté serveur + `window.location.replace('/')` côté client pour full reload. Également corrigé : turbo.json `"ui": "tui"` → `"stream"` (TUI crashait sur PowerShell), scripts `dev:hub`/`dev:client`, `'use client'` ajouté à `use-mobile.ts`, `use-system-health.ts`. | `packages/ui/src/components/{mode-toggle.tsx, mode-toggle-action.ts, mode-toggle-constants.ts, mode-toggle.test.tsx, use-mobile.ts}`, `packages/ui/src/index.ts`, `packages/modules/admin/hooks/use-system-health.ts`, `turbo.json`, `package.json` | RSC-003 : constantes depuis `'use client'` = undefined au RSC (piège silencieux majeur) | Oui → lessons-learned |
| 2026-04-20 | **get-clients crash clients disparus** : `.parse()` sur `ClientListItemSchema` lève ZodError quand un client a un champ invalide. ZodError non-sérialisable passé dans `errorResponse.details` → Next.js RSC serialization crash → onglet CRM vide. Fix : remplacé par `.safeParse()` en boucle `for...of`, les clients invalides sont skippés+loggués, les valides sont retournés. Commit `fd8d551`. | `packages/modules/crm/actions/get-clients.ts` | ZodError non-sérialisable en RSC (leçon RSC-001 confirmée) | Oui → lessons-learned |
| 2026-04-14 | **Incident Vercel — rebrand non committé** : Déploiements en fail depuis 2 jours à cause de 1172 fichiers modifiés localement (rebrand `foxeo → monprojetpro` + WIP) jamais committés dans Git. Causait un désalignement entre les imports `@monprojetpro/*` des commits récents et les packages toujours nommés `@foxeo/*` dans Git. Résolu par gros commit `chore: rebrand + sync WIP` (1190 fichiers, +12422 / -3502) + reconfiguration complète Vercel (suppression ancien projet `foxeo-one`, création de `monprojetpro-hub` et `monprojetpro-client` avec Root Directory appropriés, push des env vars via API REST sans exposer les valeurs, force deploy via `/v13/deployments` avec repoId). **Hub + Client READY en ~5 min après le commit correctif.** | 1190 fichiers (+ créations projets Vercel + archive repo `foxeo-appli-brief`) | 8 leçons regroupées sous DEP-001, GIT-001, DEP-002, DEP-003, SEC-001, CFG-002 | Oui → lessons-learned |
| 2026-07-03 | Onglet Backups branché (placeholder Story 12.2 → composant BackupStatus existant) | apps/hub/.../admin/system/page.tsx | Composant fini jamais monté — vérifier les exports de module avant de coder un placeholder | Non (couvert par audit 2026-07-03) |
| 2026-07-03 | **Onglet One aligné vision v2 (la totale)** : purge FORGE du catalogue (admin/analytics/crm/email/templates/validation-hub sortis — outils internes Hub jamais développés comme modules client ; suivi-outil ajouté), `module_catalog.family` (relation/cockpit), fin des prix mensuels à la carte, onglet « Instances One » supprimé → vue « Clients One » (offre One/One+, toggle chantier→livré, bouton Setup One complet). Au passage : `packages/modules/suivi-outil/manifest.ts` retrouvé SUPPRIMÉ du working tree (non committé) → build client rouge ; restauré via `git checkout`. Commit `1b90e7b`. | supabase/migrations/20260703220000, packages/modules/admin/* (4 créés + 8 modifiés), apps/hub/.../admin/page.tsx | Un `D` silencieux dans un working tree pollué casse le build alors que « tout est committé » — toujours `git status` sur le chemin en erreur avant de chercher ailleurs | Non (trivial une fois vu) |
| 2026-07-06 | Bascule chantier/livré : confirmation symétrique (2 directions) + notif cloche client (tool_update, auth_user_id) + mots Élio One tool_delivered/tool_construction | packages/modules/admin/{set-one-status.ts, clients-one-list.tsx, package.json}, packages/modules/elio/generate-one-concierge-word.ts | Toute bascule d'état visible client = kit complet : confirmation opérateur + cloche + mot d'Élio (pas juste le flag) | Non (pattern déjà en mémoire) |
| 2026-07-06 | Nettoyage doc « instances dédiées » : epic-list.md (Epics 1/9/10/12) + 03-core-decisions.md réalignés sur ADR-01 Rév.2 + vision v2 ; note d'obsolescence sur epic-9 stories ; §9.3 vision cochée | _bmad-output/planning-artifacts/{epics/epic-list.md, architecture/03-core-decisions.md, epics/epic-9-*.md}, docs/one-vision-v2-2026-06-24.md | Les docs de planning périmés induisent les sessions IA en erreur — réviser les docs de référence, annoter (pas réécrire) les archives | Non (hygiène doc) |
| 2026-07-06 | **Chantier Élio Hub agentique + coaching One+** (commit `fd7e33a`, 85 fichiers) : elio-chat v2 multi-LLM + tools + verify_jwt=true (faille publique fermée), agent Élio Hub 13 outils avec garde-fou `elio_hub_actions` débrayable, mémoire de conversation Hub réparée, crédits coaching One+ (ledger + config CRM + carte cockpit), calcom-webhook réécrit (insert table droppée supprimé, annulation gérée), grille 39/99 € + SubscriptionForm monté, billing-sync subscriptions + client_id, EF monthly-billing + 2 crons planifiés. Au passage : notifs visio ET billing silencieusement perdues (types hors CHECK — 3e occurrence du pattern), billing_sync absent de la publication Realtime, bug `.eq('id', clientId)` variable inexistante dans create-subscription, tier One+ jamais écrit à la graduation. | packages/modules/{elio,crm,visio,facturation,menu-facile}, supabase/{functions,migrations}, apps | Types de notification hors CHECK = pattern récurrent (3e fois) → leçon DB-006 | Oui → lessons-learned |
| 2026-07-06 | **Widget Élio sidebar v2** (commit `4542e47`) : les 4 boutons de mode étaient à moitié cosmétiques (Ordre sans action réelle, Màj Élio sans persistance, micro mort, nouvelle conversation à chaque envoi). Branchés : Ordre/Avis/Brouillon → agent outillé + garde-fou, conversation continue (sessionStorage), Màj Élio → directives persistées (system_config elio_hub_directives) injectées dans le prompt agent + section de gestion sur /elio/hub, micro → dictée vocale Web Speech fr-FR (widget + chat Hub, masqué si non supporté) | apps/hub/components/elio-query-box.tsx, packages/modules/elio/{actions,hooks,types}, apps/hub/.../elio/hub | Un bouton visible = un contrat : soit branché, soit retiré (kit complet) | Non (pattern déjà en mémoire) |
| 2026-07-07 | **Accompagnement paramétrage + chasse aux pollutions** : cloche purgée (834 alertes billing + alertes santé), anti-spam billing-sync (1 notif au seuil + rappel/24 h), health-check (alerte seulement sur error confirmé 2 cycles, debounce 60 min), sync Pennylane réparée de bout en bout (token expiré → 422 start_date → payload API 2026 non itérable → logError colonne inexistante), webhook Cal.com enregistré (créée/annulée/replanifiée + handler RESCHEDULED codé), env-probe-temp supprimée, .mcp.json sans secret en clair | supabase/functions/{billing-sync,health-check-cron,calcom-webhook}, .mcp.json | ⚠️ `supabase functions deploy` (CLI) remet verify_jwt=true par défaut — toute fonction webhook DOIT avoir un config.toml `verify_jwt = false` sinon le déploiement suivant la verrouille silencieusement | Oui → lessons-learned |
| 2026-07-10 | **Durcissement listing buckets publics** (advisor sécu 0025) : buckets `client-assets` et `screenshots` exposaient une policy SELECT rôle `public` (USING bucket_id) permettant à un anonyme d'énumérer (`list()`) tous les fichiers → découverte des userId/clientId + inventaire des captures Support d'autres users. Les 2 policies `*_select_public` supprimées. L'affichage via `getPublicUrl` (flag `public:true`, CDN hors RLS) reste intact ; aucun code n'utilise `.list()` (vérifié par grep) ; buckets quasi vides (0 + 2 fichiers). | supabase/migrations/20260710120000_harden_public_bucket_listing.sql | Un bucket `public:true` n'a PAS besoin d'une policy SELECT `public` pour l'affichage — `getPublicUrl` sert le CDN hors RLS ; la policy SELECT ne gouverne que `list()`. Une policy de listing large = énumération anonyme = fuite d'inventaire | Non (règle claire, base quasi vide) |
| 2026-07-10 | **Boucle graduation — RPC jamais déployée** : le bouton « Accéder à mon dashboard » (dernière étape onboarding One+) affichait le toast « Un problème est survenu, réessaie dans un instant ». Cause racine prouvée : la fonction `fn_mark_graduation_screen_shown` (SECURITY DEFINER) définie dans `00143` était committée sur disque/git (commit `ebe1dd3`) mais **jamais appliquée en base** → `supabase.rpc()` sur une fonction inexistante = erreur → toast → flag `graduation_screen_shown` jamais posé → boucle. Fix : déploiement de la migration 00143 en base (via apply_migration). Vérifié : fonction présente, SECURITY DEFINER, EXECUTE authenticated / refusé anon. Contrôle des 3 plans (disque/git/list_migrations) : 00143 était la SEULE migration non déployée (00135→00142 déployées, renommées en timestamp par le CLI). | supabase/migrations/00143_fn_mark_graduation_screen_shown.sql (déjà sur disque, déployé en base) | Un commit qui contient un `.sql` de fonction ne garantit PAS son déploiement — un `.rpc()` sur fonction absente = erreur générique côté UI. Toujours croiser `list_migrations` (base) vs disque après un fix DB. Renfort de la leçon « migration fichier non déployé ». | Non (leçon déjà en mémoire) |
| 2026-07-10 | **Optim RLS auth_rls_initplan** (advisor perf) : 8 policies (créées après 00136) évaluaient `auth.uid()` par ligne. Enveloppées en `(select auth.uid())` → évaluation initplan unique. Tables : billable_items, client_concierge_messages (x2), coaching_credit_ledger, tool_posts, tool_post_comments (x3). Logique inchangée (remédiation officielle Supabase). Vérifié : 8/8 wrapped, 0 appel nu. | supabase/migrations/20260710170000_perf_wrap_auth_uid_remaining_rls_policies.sql | Toute nouvelle policy RLS doit d'emblée utiliser `(select auth.uid())` — sinon l'advisor la re-signale à chaque ajout de table | Non (optim, base quasi vide) |
| 2026-07-10 | **Cohérence prix module « inclus »** : éditer un module Relation + « One de base » laissait saisir un « Prix setup HT » qui n'apparaissait jamais dans le tableau (colonne affiche « Inclus » en dur pour ces modules) → MiKL croyait à un bug de rafraîchissement. En réalité : écriture DB OK, règle d'affichage volontaire. Fix cohérence : dans la modale d'édition, le champ prix est désactivé (+ message) quand le module est « One de base », et forcé à 0 à l'enregistrement. Normalisation DB : prix fantômes des modules inclus remis à 0 (documents 500→0). Au passage : `UPDATE` module_catalog via MCP filtré en silence par RLS (`is_operator()` faux hors session opérateur) — normal, le rôle propriétaire des migrations passe outre. | packages/modules/admin/components/module-edit-modal.tsx | Un champ éditable dont la valeur est ignorée à l'affichage = piège de cohérence (l'utilisateur croit à un bug de sync). Désactiver la saisie quand la valeur n'a pas d'effet, ne pas juste la masquer en lecture. | Non (règle claire) |
| 2026-07-10 | **Régression vue Lab client gradué** : un client gradué en MODE LAB voyait un résumé condensé lecture seule (`LabHistoryView`, raccourci ajouté au Lot 3 / commit c96fe4a) au lieu du parcours entier grisé attendu. Comportement agréé restauré : retrait du raccourci `labReadOnly → LabHistoryView` dans `modules/parcours/page.tsx` → les gradués passent par le même `ParcoursPageClient` que les Lab natifs, avec `agentsPaused = !elio_lab_enabled` → parcours grisé/en pause, Élio Concierge « en pause », étapes cliquables (conversations + docs consultables), bouton « Générer » masqué, réouverture réservée à MiKL depuis le Hub. `LabHistoryView` (composant + test) conservé mais plus référencé. | apps/client/app/(dashboard)/modules/parcours/page.tsx | Un « Lot » qui ajoute un raccourci de rendu (consultation) peut court-circuiter en silence un état plus riche déjà construit ailleurs (parcours grisé) → régression invisible en tests. Quand un état visuel existe déjà, le réutiliser plutôt qu'ajouter une vue dépouillée parallèle. | Oui → lessons-learned |
| 2026-07-10 | **UX parcours Lab en pause — 2 fixes** : (1) **Vert One dans le Lab** : `layout.tsx` surchargeait le token shadcn `--accent` avec la couleur de branding du client (Dev Test = #1fa216 vert) dans TOUS les modes → en MODE LAB le chip dossier « Tous les documents » (`bg-accent`) devenait vert au lieu du violet du thème. Surcharge `--accent` désormais limitée à `activeMode === 'one'`. (2) **Cards « en cours » alors que le Lab est en pause** : `ParcoursStepCard` n'affichait le variant « En pause » que sur `isAbandoned` (parcours abandonné), pas sur `agentsPaused` (Lab en pause) → les étapes non finalisées restaient « En cours »/« Continuer ». Ajout prop `isPaused` (= agentsPaused) : variant « En pause » + « Consultation uniquement » pour les étapes non complétées ; les complétées gardent leur badge vert « Complétée » (garde `status !== 'completed'`). | apps/client/app/(dashboard)/layout.tsx, packages/modules/parcours/components/{parcours-step-card.tsx, parcours-overview.tsx} | Une couleur de branding client injectée dans le token générique `--accent` bave sur TOUS les modes/thèmes — scoper l'override de token thème au mode concerné. Un état visuel (pause) doit être piloté par le bon flag (agentsPaused), pas seulement par un cas voisin (isAbandoned). | Non (patterns clairs) |
| 2026-07-10 | **Parcours en pause — cohérence visuelle (suite)** : en pause, tout le parcours doit lire « gelé ». (1) Card complétée : version atténuée/grisée (opacity-70, badge « Complétée » discret, titre muté) au lieu du vert vif qui la faisait paraître active. (2) Cards non complétées : badge « En pause » → « Non finalisée » + footer « En pause · consultation uniquement » (indique l'incomplétude, pas seulement la pause). (3) Barre de progression atténuée (opacity-50) en pause. (4) Bandeau « Parcours libre » masqué en pause (il incitait à avancer alors que tout est gelé). | packages/modules/parcours/components/{parcours-step-card.tsx, parcours-overview.tsx} | En état « gelé », TOUS les éléments actifs (cartes complétées, barre de progression, incitations à avancer) doivent être atténués de façon cohérente — un seul élément resté « plein phare » suffit à faire croire que le parcours est encore actif. | Non (affinage UX) |
| 2026-07-10 | **Suppression LabHistoryView (code mort)** : composant + test + exports retirés (`packages/modules/core-dashboard`) après le retrait de son dernier usage (parcours gradué → parcours grisé). Vérifié : 0 référence restante, types `ParcoursData`/`ParcoursStep` réexportés importés nulle part ailleurs. NB : 2 échecs pré-existants dans `core-dashboard.test.ts` (quick-access cards) — sans rapport, confirmés présents sur HEAD avant ce commit. | packages/modules/core-dashboard/{index.ts, components/lab-history-view.tsx (suppr), components/lab-history-view.test.ts (suppr)} | — | Non (nettoyage) |
| 2026-07-10 | **Tests obsolètes CoreDashboard retirés** : 2 tests de rendu (`core-dashboard.test.ts`) asseraient la grille « Accès rapide » (Chat/Documents) et le message « Contactez MiKL » — UI supprimée volontairement lors de la refonte Vision One v2 (2026-06-25, la sidebar assure la navigation). Tests devenus faux → retirés. Module core-dashboard : 49/49 verts. | packages/modules/core-dashboard/components/core-dashboard.test.ts | Après un refacto qui retire de l'UI, purger les tests de rendu correspondants dans le même commit — sinon ils rougissent en dette silencieuse. | Non (dette de test) |
| 2026-07-10 | Refonte cockpit tout le Hub (socle @monprojetpro/ui/cockpit + 12 modules) | packages/ui/src/components/cockpit/*, ~78 fichiers Hub | Guillemets courbes agents cassent next build ; fausse alerte git sur tree partagé | Oui → UI-006 |
| 2026-07-15 | **Avatars agents Élio invisibles dans le Hub (sauf Élio Dev)** : les 14 PNG d'agents n'existaient que dans `apps/client/public/elio/agents/` — le catalogue Élio Lab est servi par le Hub → 404 sur chaque image → fallback icône robot. Copie des PNG dans `apps/hub/public/elio/agents/` (+ suppression du fox-placeholder.svg orphelin). | apps/hub/public/elio/agents/*.png | Un asset `public/` n'est PAS partagé entre les apps du monorepo : chaque app Next sert son propre dossier public — tout visuel affiché par les deux apps doit exister dans les deux `public/`. | Oui → lessons-learned |
| 2026-07-15 | **Boucle de redirection infinie Mode Lab (client Dev Test)** : mode Lab actif (cookie + lab_mode_available) mais module `parcours` absent d'active_modules → `/` redirigeait vers `/modules/parcours` (garde de mode) qui redirigeait vers `/` (requireActiveModule) → alternance accueil/« Application error » en continu. Fix données : `parcours` ajouté aux active_modules de Dev Test. Fix code : la home ne redirige vers le parcours que si le module est réellement actif. | apps/client/app/(dashboard)/page.tsx + UPDATE client_configs (Dev Test) | Deux gardes de redirection qui vérifient des conditions différentes (mode vs modules actifs) peuvent se renvoyer la balle à l'infini — toute redirection doit vérifier que sa CIBLE est atteignable. | Oui → lessons-learned |
| 2026-07-16 | **Fiche client : design cockpit sur les 11 onglets** (vague 4 agents Sonnet — Pilote, Historique, Documents, Échanges, Branding, One, Doc Élio, Paramètres, Emails, Support, Soumissions, Élio) + **UX levier Lab** : badge d'état « Actifs / En pause » sur le switch Agents du parcours, pastille Lab du header cliquable → onglet Lab. | 13 composants (crm, elio, email, parcours, support) + access-toggles.tsx, client-header.tsx | Protocole UI-006 respecté (scan guillemets, build global 2/2 vert, agents sans build/commit individuels) — RAS. | Non (protocole existant) |
| 2026-07-16 | **Règle « One déclenché ⇒ Lab en pause » + état du Lab cohérent** : ouvrir l'Accès One coupe automatiquement les agents du parcours (elio_lab_enabled=false + suspension du parcours), réversible via le switch (« sauf si j'en décide autrement » — décision MiKL). L'en-tête « État du Lab » de l'onglet Lab lisait dashboard_type (→ « Lab non activé » avec agents « Actifs » juste en dessous) ; il lit désormais les vrais flags → 3 états : non activé / actif / en pause. | packages/modules/crm/{actions/toggle-access.ts, components/client-lab-tab-content.tsx, components/access-toggles.tsx} + tests | Deux affichages du même concept adossés à deux sources différentes (dashboard_type vs flags) = incohérence garantie ; toujours une source unique. Une règle métier « quand X alors Y » doit être posée dans TOUTES les actions qui déclenchent X (ici : graduation ✅ déjà fait, toggle Accès One ❌ manquant). | Oui (règle métier durable) |
| 2026-07-16 | **Posture « Collaboration d'abord » des 12 agents Élio Lab** : les agents balançaient leur catalogue dès le 1er message du client (constat MiKL sur Élio Dev). Bloc ajouté aux 12 prompts : 1 question d'écoute adaptée au domaine → reformulation → propositions par blocs de 3-4 ancrées dans les mots du client. Force de proposition conservée (pas de retour aux coachs passifs). Fichiers .md + sync direct en base (effectif immédiatement). | packages/modules/elio/agents/lab/*.md + elio_lab_agents (DB) | — | Non (évolution de posture, pas un piège) |
| 2026-07-16 | **Élio Dev « En attente » sur parcours libre + doublon sidebar Lab** : (1) `addParcoursStep` insérait toujours en `pending` sans lire `client_configs.parcours_mode` → une étape ajoutée après le lancement restait « En attente » alors que tout le parcours libre est ouvert ; désormais libre → `active`, tracé → `pending` (même règle que launch-client-parcours). Donnée Dev Test corrigée en base. (2) Sidebar Lab : entrée « Dashboard » retirée — en Lab, `/` redirige vers Mon Parcours, le lien était un doublon inerte (« ça veut pas cliquer »). | packages/modules/parcours/actions/add-parcours-step.ts (+test), packages/ui/src/components/module-sidebar.tsx | Une règle d'initialisation d'état (statuts selon parcours_mode) doit être appliquée par TOUTES les actions qui créent l'entité (launch ✅, add ❌ oublié) — même famille que la leçon toggle One/graduation du même jour. | Non (déjà capitalisé le matin — pattern identique) |
| 2026-07-17 | **Panneau « Instance One » du cockpit refait en « Dashboard One »** : le panneau lisait client_instances (concept hérité instance-per-client, vide en multi-tenant) → « Pas encore d'instance One » alors que l'Accès One était ouvert. Refonte : statut basé sur le vrai levier one_mode_available + compteur de modules actifs (client_configs) + toggle « Accès One » déplacé dans le panneau (nouveau OneAccessToggle) ; AccessToggles retitré « Accès Lab » (volet Lab uniquement) ; mention « instance dédiée » réservée au kit de sortie. | packages/modules/crm/components/{client-cockpit-tab.tsx, one-access-toggle.tsx, access-toggles.tsx, client-lab-tab-content.tsx} + tests, docs/guide.md | Même famille que la leçon du 2026-07-16 : un panneau adossé à une source (client_instances) et un toggle adossé à une autre (client_configs) pour le même concept = incohérence garantie. | Non (pattern déjà capitalisé la veille) |
