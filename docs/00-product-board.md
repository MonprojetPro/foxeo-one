# Product Board — MonprojetPro (Hub · Lab · One)

> Source de verite produit, tenue par OTTO. Reconstitue le 2026-08-24 depuis CLAUDE.md, docs/ et 756 commits git.
> **Regle d'or : un identifiant est immortel. On change son statut, jamais son existence.**

| | |
|---|---|
| Projet | MonprojetPro (ex-Foxeo/Foxio) — Hub (operateur MiKL) + Lab (parcours accompagne) + One (dashboard client livre) |
| Proprietaire | MonProjetPro — produit interne, pas un projet client |
| Phase | Build (en production sur monprojet-pro.com, developpement continu) |
| Derniere mise a jour | 2026-08-31 par OTTO |
| Prochain jalon | [a confirmer par MiKL] |

---

## 1. Vision en 5 lignes

MonprojetPro est une plateforme modulaire de dashboards professionnels (monorepo Turborepo) qui outille
la relation MiKL <-> ses clients : **Hub** (pilotage operateur), **Lab** (parcours d'accompagnement
guide par les agents Elio), **One** (dashboard livre au client, abonnement recurrent). Vision v2
(2026-06-24) : le One est « la console de pilotage des livrables du client + le canal de lien
permanent avec MiKL » — un socle **Relation** universel surmonte de **Cockpits** sur-mesure par
projet. Reussi si le client reste dans son One (fidelisation) et si chaque module nourrit une
bibliotheque reutilisable (doctrine FORGE) plutot que d'etre recode. [a confirmer par MiKL]

---

## 2. INBOX — idees brutes non triees

| ID | Idee | Origine | Date | A qualifier avec |
|---|---|---|---|---|
| T-024 | 🔴 **SECURITE — les 2 webhooks du Hub sont OUVERTS en production.** PROUVE PAR SONDE le 16-09, pas suppose : `POST /api/webhooks/contact-form` sans signature -> **HTTP 400 « name and email are required »** (la verification de signature a ete FRANCHIE) et `POST /api/webhooks/cal-com` sans signature -> **HTTP 200 `{received:true}`**. Cause : les deux routes portent `if (!secret) return true` — « accepte tout si pas de secret configure », un confort de developpement qui, en production, transforme un secret absent en porte ouverte. Consequence : n'importe qui peut injecter des prospects dans le CRM et des reunions dans l'agenda, en service role (RLS contournee). AGGRAVANT COMPTABLE : un booking cree des `billable_items` a 45 € (coaching hors forfait) — donc un tiers peut fabriquer des lignes facturables. SECOND CONSTAT : il existe DEUX recepteurs du webhook Cal.com aux comportements de securite OPPOSES — l'Edge Function `calcom-webhook` refuse proprement (500 sans secret, 401 sans signature) et porte la logique de facturation, la route Next l'accepte. Savoir lequel Cal.com appelle vraiment fait partie du sujet | Sonde MAX pendant la verif T-003 | 2026-09-16 | MiKL — a traiter AVANT la bascule en facturation reelle |
| T-025rev | 🔎 **REVISION DU DIAGNOSTIC T-025, le 2026-09-17 — 2 constats sur 6 se degonflent a l'inspection du code.** Ecrit ici parce qu'un diagnostic faux laisse dans le board est pire que pas de diagnostic. ❌ **Constat 5 ABANDONNE** (« les devis ne sont plus synchronises, c'est un oubli ») : ce n'est PAS un oubli mais un choix d'architecture, documente dans le code depuis le 2026-04-15 (`create-quote.ts:149-153`). Pennylane n'expose de changelog que pour `customer_invoices` et `customers` — **il n'existe aucun `/changelogs/quotes`**. Les devis sont donc ecrits EN MIROIR dans `billing_sync` au moment ou le Hub les cree (`create-quote`, `update-quote`, `cancel-quote`, `send-quote-by-email`). Ce qui reste vrai et doit etre traite : la ligne `quote` de `billing_sync_state`, figee au 10-02, est un **vestige trompeur** — c'est elle qui m'a fait conclure a une panne. LIMITE REELLE qui subsiste : un devis cree directement dans Pennylane, hors du Hub, ne remontera jamais. ❌ **Constat 6 ABANDONNE** (« doublon de detection des impayes ») : les deux fonctions ne font pas le meme travail. `billing-sync` ALERTE MiKL (`shouldSendCriticalAlert`) ; `detect-overdue-invoices` prepare des **brouillons de relance client via Elio**, avec niveaux de relance et anti-doublon (`getReminderLevel`, `hasExistingReminder`, `buildReminderPrompt`). Deux roles distincts, aucune suppression a faire. ✅ **Constat 2 RENFORCE** : le champ `data` des documents ne porte que 4 a 6 cles (`date`, `label`, `currency`, `invoice_number`) la ou une reponse reelle de l'API Pennylane en porte des dizaines. **Preuve positive qu'ils sont fabriques a la main** — donc ils n'existent pas dans Pennylane, et les supprimer du Hub n'affectera pas la comptabilite reelle. ⛔ **LECON : j'ai produit ces 6 constats en une passe, par requetes en base et lecture rapide. Deux etaient faux. Un diagnostic n'est pas une preuve — la preuve, c'est la verification de chaque point AVANT d'agir dessus.** | Revision MAX | 2026-09-17 | — |
| T-025 | **Remettre a plat toute la comptabilite du Hub avant de facturer pour de vrai.** MiKL : « la comptabilite du Hub est fausse, je vais commencer a facturer pour de vrai, je veux que tout marche impeccable ». **DIAGNOSTIC POSE LE 16-09, PAR REQUETES REELLES EN BASE — 6 constats :** (1) **La totalite de la compta affichee est un jeu de demonstration.** Les 3 seuls clients (`Atelier Reynaud`, `Maison Vasseur`, `Le Comptoir de Camille`) sont declares FICTIFS dans `docs/vitrine-screenshots.md:12-13`, qui precise meme qu'ils sont « destines a etre supprimes une fois les captures faites ». Deux portent un UUID fabrique (`a1000000-…-0001`, `b2000000-…-0002`). (2) **Les 16 documents de `billing_sync` (8 factures, 6 devis, 2 abonnements) n'ont PAS pu venir de Pennylane** : ils sont tous rattaches a un client, alors que `clients.pennylane_customer_id` est **NULL pour les 3 clients** — or c'est par cette colonne que le `ClientResolver` de la synchro rattache. Un document rapatrie serait donc orphelin. Ils ont ete inseres a la main ou par un script ponctuel ; aucun fichier de seed ne les porte (`grep` sur tout le depot : seule `docs/vitrine-screenshots.md` les nomme). (3) **Pennylane est bien en PRODUCTION, pas en bac a sable** — voir l'entree de journal dediee. (4) **Defaut d'idempotence sur l'accrual des credits coaching** : `coaching_credit_ledger` porte DEUX `monthly_accrual` a un jour d'ecart, le **2026-07-31 et le 2026-08-01**, alors que le cron est `0 5 1 * *` (le 1er a 5 h) — un accrual date du 31 ne peut pas en venir. A comprendre avant de facturer au credit. (5) **Les devis ne sont plus synchronises depuis le 2026-02-10** : `entityConfigs` (`billing-sync/index.ts:962-967`) ne liste que `invoice`, `customer`, `subscription` — le type `quote` n'y figure pas, et `billing_sync_state` affiche pourtant `consecutive_errors: 0`. Sept mois de silence sans une seule alarme. (6) **Doublon de detection des impayes** : `billing-sync` (toutes les 5 min) porte `isInvoiceOverdue`/`shouldSendCriticalAlert`, ET l'Edge Function `detect-overdue-invoices` tourne en plus tous les jours a 8 h 10 (`cron.job` verifie : les 3 crons compta sont `active: true`). Risque de double notification | MiKL 2026-09-16 | 2026-09-16 | MiKL — niveau de purge a trancher |

| T-026 | **Marquer les clients et la compta de demonstration COMME FICTIFS dans l'application.** MiKL 2026-09-16, en reponse a la question « on supprime les 3 clients fictifs ? » : « non je voudrais les garder, mais faudrait les marquer dans l'application comme fictif, on ne sait jamais si je dois montrer l'application a des gens ». CONTEXTE QUI PESE SUR LA FORME : MiKL est en phase d'**accreditation CII** sur ce projet — il montre donc l'application a des evaluateurs. Le marquage doit etre **honnete et non equivoque pour un tiers** (c'est tout son interet : ne jamais laisser croire que ce sont de vrais clients) **sans faire passer le produit pour une maquette**. Renverse la decision par defaut annoncee le meme jour (« on les supprime avec toute leur compta »), et devient donc un PREALABLE a toute purge : ce qui est marque n'a plus besoin d'etre efface. Lie a `docs/vitrine-screenshots.md:12-15`, qui interdit deja de les presenter comme de vrais temoignages | MiKL 2026-09-16 | 2026-09-16 | MiKL — forme du marquage a valider (ou il apparait, comment il est formule) |

| T-024a | **Les RDV Cal.com n'apparaissent pas dans l'agenda du Hub.** Reste-a-faire de T-024, decouvert par l'inspection des consumers avant de supprimer la route morte — **bug preexistant, pas une regression introduite**. `agenda-page.tsx:169` appelle `getCalcomBookings` (`actions/calendar.ts:267`), qui lit la table `calcom_bookings` ; or cette table n'etait alimentee que par la route Next supprimee, et elle est **vide depuis toujours**. Le vrai recepteur, l'Edge Function `calcom-webhook`, ecrit dans `meetings` (`index.ts:76`, `:197`, `:261`, `:278`). Les deux moities de la chaine ne se sont jamais parle. A trancher au moment de le traiter : faire lire `meetings` a l'agenda (le plus simple, une seule source), ou faire alimenter `calcom_bookings` par l'Edge Function (conserve la separation RDV externes / reunions internes). Une fois choisi, `getCalcomBookings` et la table `calcom_bookings` deviennent peut-etre du code mort a retirer | Filiation T-024 | 2026-09-17 | MiKL — a qualifier apres le chantier compta |

> 4 idees en attente de tri. T-024 est un point de securite actif en production, pas une idee.

---

## 3. Backlog qualifie

| ID | Titre | Type | Origine | Priorite | Perimetre | Valeur | Effort | Statut | Epic | Cree | Cible | MAJ |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| F-001 | Gate FORGE (7 portes) sur les 9 modules Relation du socle One | Feature | Vision v2/FORGE | Should | Interne | H | L | Qualifie | — | 2026-06-24 | — | 2026-08-24 |
| F-002 | Premier Cockpit produit reel (Site / App) | Feature | Vision v2 | Should | A qualifier | H | L | Qualifie | — | 2026-06-24 | — | 2026-08-24 |
| F-003 | Webhooks & API publique One (placeholders "Phase 2") | Feature | Audit 07-03 | Could | A qualifier | M | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| F-004 | Support multi-operateur (app centree MiKL seul) | Exploration | Audit 07-03 | Could | Interne | M | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| F-005 | Posture Elio One (usage vs collecteur d'evolutions) | Amelioration | Vision v2 §9 | Should | Interne | M | M | Qualifie | — | 2026-06-24 | — | 2026-08-24 |
| F-006 | Sources de metriques cockpits — a decider par projet | Exploration | Vision v2 §9 | Could | Interne | M | L | Qualifie | — | 2026-06-24 | — | 2026-08-24 |
| T-001 | `elio-chat` ouverte au public (pas d'auth, CORS `*`) | Dette technique | Audit secu 07-03 | Must | Interne | H | M | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-002 | Policy `notifications_insert_system` trop ouverte | Dette technique | Audit secu 07-03 | Must | Interne | H | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-003 | Webhooks cal-com/contact-form fail-open si secret absent | Dette technique | Audit secu 07-03 | Should | Interne | M | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-004 | 32 fonctions SECURITY DEFINER exec. par anon + grant PUBLIC | Dette technique | Audit secu 07-03 | Should | Interne | M | M | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-005 | Buckets `client-assets`/`screenshots` publics et listables | Dette technique | Audit secu 07-03 | Should | Interne | M | M | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-006 | Aucun header securite (CSP/HSTS) + `ignoreBuildErrors:true` | Dette technique | Audit secu 07-03 | Should | Interne | M | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-007 | `npm audit` — dompurify/postcss/xmldom severite high | Dette technique | Audit secu 07-03 | Should | Interne | M | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-008 | 2 migrations en base sans fichier `.sql` dans le repo | Dette technique | Audit secu 07-03 | Must | Interne | H | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-009 | OAuth Gmail — `state` non signe (CSRF) | Dette technique | Audit secu 07-03 | Should | Interne | M | M | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-010 | Edge Function `env-probe-temp` active en prod, sans source | Dette technique | Audit secu 07-03 | Must | Interne | M | L | Qualifie | — | 2026-07-03 | — | 2026-08-24 |
| T-011 | Chaine de controles CI (doc, tests, isolation multi-tenant) | Dette technique | Dossier CII 08-22 | Must | Interne | H | M | Livre (isolation -> T-015) | — | 2026-08-22 | — | 2026-08-24 |
| T-012 | Migration 00094 fantome — remettre la base en coherence avec ses migrations | Dette technique | CI T-011 | Should | Interne | M | S | En attente MiKL | — | 2026-08-24 | — | 2026-08-24 |
| T-013 | 40 tests desynchronises du code | Dette technique | CI T-011 | Should | Interne | M | M | Livre | — | 2026-08-24 | — | 2026-08-24 |
| T-014 | Aucune config ESLint (15 packages la declarent) | Dette technique | CI T-011 | Should | Interne | M | M | Livre | — | 2026-08-24 | — | 2026-08-24 |
| T-015 | Droits anon/authenticated absents sur base reconstruite | Dette technique | CI T-011 | Must | Interne | H | M | Qualifie | — | 2026-08-24 | — | 2026-08-24 |
| F-010 | Synchronisation Gmail des relances du comptable (Story 13-9, jamais mise en service) | Feature | CI T-011 | Could | A qualifier | M | M | Bloque — entrees MiKL | — | 2026-08-24 | — | 2026-08-24 |
| T-018 | SPF de monprojet-pro.com obsolete (OVH) — n'autorise pas Resend, ne correspond plus au MX reel (Google). Fait DNS reel, mais fausse piste sur l'incident du 09-01 (cause reelle : Outlook local MiKL) | Dette technique | MiKL 2026-09-01 (signalement sans email recu) | Should | Interne | M | S | Qualifie — DNS chez MiKL, sans urgence | — | 2026-09-01 | — | 2026-09-01 |
| T-016 | Upload de capture d'ecran bloque en silence dans "Signaler un probleme" (Lab) | Bug | MiKL 2026-08-31 | Must | A qualifier | H | S | Livre | — | 2026-08-31 | 2026-08-31 | 2026-08-31 |
| F-011 | Rendre "Signaler un probleme" visible dans le Lab (bouton + onglet Mes signalements, icone alerte header) + suppression page orpheline /support | Feature | MiKL 2026-08-31 | Should | A qualifier | M | S | Livre | — | 2026-08-31 | 2026-08-31 | 2026-08-31 |
| F-013 | Kit FORGE « fil de discussion complet » : fil a 2 sens, pieces jointes visibles dans le fil, reecriture ET correction orthographe/syntaxe par l'IA (meme fonction qu'aujourd'hui, prompt fourni par le projet), temps reel par defaut avec repli signale a l'utilisateur quand la source ne le permet pas. S'appuie sur les kits deja extraits `pieces-jointes-supabase` et `realtime-refresh-supabase-next` plutot que de recopier | Feature | MiKL 2026-09-08 | Should | Interne | H | L | Livre | — | 2026-09-08 | 2026-09-08 | 2026-09-08 |
| T-022 | Un nouveau message MenuFacile n'est visible NULLE PART hors du module : pas de badge sur l'entree « MenuFacile » de la sidebar (alors que Chat et Comptabilite en ont un), et absent de « Messages non lus » de l'accueil — qui ne lit que la table `messages` du chat MPP, pas le guichet MenuFacile. Consumers oublies lors de l'ajout du module | Bug | MiKL 2026-09-09 (2 captures) | Should | Interne | M | M | Livre — badge sidebar (T-022) + accueil deja couvert par la tuile « Mes projets » de F-012 (verifie sur capture MiKL 11-09) | — | 2026-09-09 | — | 2026-09-11 |
| T-020a | Reste-a-faire de T-020, dette sans impact utilisateur : (1) `apps/hub/app/(auth)/login/page.tsx` — 2e page de login, filet de securite documente si `SUPABASE_SERVICE_ROLE_KEY` manque cote client, mais visuellement datee (pas de branding, pas de « mot de passe oublie ») : la neutraliser en simple relais vers l'entree unique, ou l'assumer ? (2) `apps/client/app/api/auth/callback/route.ts` — route dupliquee, commentee comme inatteignable dans son propre code : a supprimer | Dette technique | Audit T-020 2026-09-09 | Could | Interne | L | S | Qualifie |  — | 2026-09-09 | — | 2026-09-09 |
| T-022a | Reste-a-faire de T-022 : rendre les messages MenuFacile visibles sur l'ACCUEIL. SANS OBJET — deja livre le 08-09 par F-012. Capture MiKL du 11-09 : le coin « Mes projets » de l'accueil porte la tuile MenuFacile avec « Messages non traites : 2 ». La question posee (compteur unique OU deux lignes) etait donc deja tranchee dans les faits : DEUX lignes, dans deux panneaux distincts. Erreur d'analyse a la convergence de T-022 : j'ai cherche les messages MenuFacile dans le panneau « Messages non lus » sans verifier si un autre bloc de la MEME page les portait deja | Bug | Convergence T-022 2026-09-09 | Should | Interne | M | S | Sans objet — couvert par F-012 |  — | 2026-09-09 | — | 2026-09-11 |
| T-020 | Redirections vers l'authentification : tout ne pointe PAS vers la page de connexion unique. Constate par MiKL sur les boutons des emails (« Voir le message ») qui ramenent sur l'ancienne page de login. Audit complet a faire : emails, edge functions, middlewares, actions, liens en dur | Bug | MiKL 2026-09-09 (2 captures) | Must | Interne | H | M | Livre — reste T-020a | — | 2026-09-09 | — | 2026-09-09 |
| T-021 | Chat : l'abonnement temps reel ne lit pas son statut (se fige en silence si la table sort de la publication ou si la RLS bloque) ET l'envoi optimiste depend de ce meme abonnement pour se resoudre. Trouve par l'inventaire du kit F-013 | Dette technique | Inventaire F-013 2026-09-08, feu vert MiKL 2026-09-09 | Should | Interne | M | S | Livre | — | 2026-09-08 | — | 2026-09-09 |
| F-015 | Accueil Hub v3 — cockpit DEUX COLONNES : a gauche le travail (A traiter, puis Radar), a droite une colonne laterale fixe (chiffres en lignes + projets identifies par une pastille de couleur). Corrige les 5 griefs de la v2 : encarts trop gros, « a traiter » trop bas, clients/MRR pas prioritaires, Radar minuscule et en fin de page, projets mal identifies | Amelioration | MiKL 2026-09-08 (« l'ensemble ne me va pas, propose moi autre chose ») | Should | Interne | M | M | Livre | — | 2026-09-08 | 2026-09-08 | 2026-09-08 |
| F-014 | Refonte visuelle de l'accueil du Hub : 3 zones (Pilotage cyan, A traiter ambre, Radar gris), code couleur porteur de sens, grille KPI reparee, etats vides compactes, Radar repliable. Proposition PIXEL, lot complet arbitre par MiKL | Amelioration | MiKL 2026-09-08 (« ca commence a etre un peu le bazar ») | Should | Interne | M | M | Livre | — | 2026-09-08 | — | 2026-09-08 |
| F-012 | Coin "Mes projets" sur l'accueil du Hub : tuiles configurables par projet (MiKL coche ce qu'il veut voir). Premier occupant MenuFacile : messages non traites, foyers inscrits, foyers actifs 30j | Feature | MiKL 2026-09-08 | Should | Interne | M | M | Livre | — | 2026-09-08 | 2026-09-08 | 2026-09-08 |
| T-019 | Zone de saisie des reponses : le texte deborde de la pop-up et les boutons d'envoi deviennent inatteignables (toutes les `Textarea` du produit) | Bug | MiKL 2026-09-08 (capture, fil MenuFacile) | Must | Interne | H | S | Livre | — | 2026-09-08 | 2026-09-08 | 2026-09-08 |
| T-017 | Signalement Lab : jusqu'a 3 pieces jointes (au lieu d'1), compression image avant upload, upload direct navigateur->Supabase (pattern GuardVeto) | Amelioration | MiKL 2026-08-31 | Should | A qualifier | M | M | Livre | — | 2026-08-31 | 2026-08-31 | 2026-08-31 |
| T-023 | Pieces jointes MenuFacile invisibles dans le Hub : une cliente (Lucie, HIRVOAS LOTRIAN FAMILY) joint des captures d'ecran depuis l'app MenuFacile, le guichet les transporte, mais le fil du Hub n'affiche que `body` — les fichiers sont ignores en silence. Cause : le contrat consomme cote Hub (`ContactMessage`, `ContactThreadMessage`) n'a aucun champ fichier, et F-013 a explicitement decide de ne PAS appliquer le kit `fil-de-discussion-complet` aux implementations existantes. MiKL confirme : oubli cote Hub, pas cote MenuFacile. Perimetre elargi par MiKL le 10-09 : les DEUX sens (recevoir les fichiers de l'utilisateur ET pouvoir en envoyer depuis le Hub). Contrat d'API demande cote MenuFacile (instructions passees par MiKL a la session Claude Code du projet MenuFacile). MAJ 11-09 : MenuFacile renvoie la balle (« c'est de ton cote que ca se joue »). VERIFIE dans le code : `types/index.ts:256` `ContactThreadMessage = { sender, body, created_at }` — aucun champ fichier, et zero occurrence de attachment/file_url/piece jointe dans tout le module et sa doc. Donc si le guichet envoie deja les fichiers, le Hub les jette en silence et MenuFacile a raison. Reste a PROUVER par la reponse reelle du guichet — sonde ecrite (scratchpad `probe-guichet.mjs`), bloquee : `MENUFACILE_ADMIN_API_URL` absente du `.env.local` (seul le SECRET y est), et le pull des variables de production Vercel est refuse par le garde-fou secrets. **MAJ 15-09 : DEBLOQUE et code cote Hub.** Le guichet MenuFacile a livre son volet le 10-09 (admin-api v13, ACTIVE) : `attachments[]` decrits par message, `attachment_count` sur la liste, endpoint de renouvellement d'URL. Le Hub les consomme desormais (types + `contact-attachments.tsx` + badge liste). Le sens « recevoir » est donc FAIT au code ; ne reste que la preuve par un appel reel et le sens « envoyer » (T-023a) | Bug | MiKL 2026-09-10 (capture, message Lucie du 10/09 11:32) | Must | Interne | H | M | **Livre** — deploye et confirme de visu par MiKL le 15-09 (voir section 4) | — | 2026-09-10 | 2026-09-15 | 2026-09-15 |
| T-023a | Repondre AVEC des fichiers depuis le Hub (2e sens du perimetre elargi par MiKL le 10-09). Bloque en amont : le guichet MenuFacile n'expose pas encore d'URL d'upload pre-signee ni `attachment_ids` sur `/reply` — c'est l'item F-030c du board MenuFacile, non demarre. Piege deja identifie la-bas : les policies Storage ne laissent lire que le dossier `<auth.uid()>/…`, donc un fichier depose par l'operateur ailleurs serait invisible cote utilisateur. MAJ 15-09 : blocage RE-VERIFIE PAR SONDE REELLE (et non repris du board — leçon de T-023 ou l'inverse s'etait produit). Le guichet s'auto-declare `menufacile-admin-api` version 3 sur `GET /` : 24 endpoints, AUCUN d'upload ; `/contact-messages/:id/attachments/upload-url` et `/contact-messages/:id/attachments` -> HTTP 404 ; zero message `sender:"admin"` portant des fichiers. Spec complete ecrite pour MenuFacile : `packages/modules/menu-facile/docs/spec-attachments-operateur.md` (contrat de lecture a ne pas casser, 2 endpoints demandes, piege Storage, contraintes, protocole de validation). **DEBLOQUE le 15-09 dans la journee : MenuFacile a livre F-030c** — les 2 endpoints existent, guichet **v16 ACTIVE**, 21 verifications sans echec de leur cote. Leur reponse detaillee : `packages/modules/menu-facile/docs/spec-attachments-operateur-REPONSE.md`. A lire AVANT de coder, 4 ecarts avec notre spec : la reponse rend aussi un `token` (necessaire si on passe par `uploadToSignedUrl`), `expires_in` vaut **7200** et non 600, l'extension du fichier est imposee par le `mime_type` declare, et le plafond REEL est **10 Mo** (celui du bucket) — nos 5 Mo sont a appliquer ici, cote Hub, car leur controle de taille est declaratif. Le piege Storage est traite sans elargir aucune policy : ils deposent dans le dossier du destinataire, parce que `get_my_contact_thread` ne rend un fil qu'a son auteur. Reste ici : selecteur de fichiers, compression, upload, affichage — et la verification depuis un vrai compte foyer, qu'ils ne pouvaient pas faire faute d'identifiants | Feature | Filiation T-023 | Should | Interne | M | S/M | **Livre** — confirme par MiKL sur la prod le 16-09 | — | 2026-09-15 | — | 2026-09-16 |

> Perimetre `Interne` = dette invisible du client (jamais `Devis` par defaut, regle OTTO).
> Statuts "Qualifie" figes faute de suivi ecrit — a confirmer MiKL ce qui est deja traite depuis 07-03.

---

## 4bis. Convergence — controle de sortie avant commit

> **Rempli par OTTO avant CHAQUE commit qui contient du code.** Une ligne par element annonce.
> C'est la seule section que le harnais lit pour autoriser un commit : sans entree fraiche
> pour l'item concerne, `gate-commit` refuse. Ce n'est pas de la paperasse — c'est la reponse
> a la seule question qui compte : *a-t-on livre ce qui etait demande, en ENTIER ?*

| ID | Ce qui etait annonce | Ce qui est reellement livre | Verdict |
|---|---|---|---|
| T-026 | **Marquage demo porte par la DONNEE, pas par une liste d'identifiants dans le code** | Migration `20260920100000_clients_is_demo.sql` : colonne `is_demo boolean not null default false` + index partiel. Appliquee en production (`apply_migration` -> `success`). Verifie APRES : les 3 clients de demonstration ressortent `is_demo: true`, et aucun autre. Une liste en dur aurait ete fausse des qu'on change de base (preview, kit de sortie) | OK |
| T-026 | **Le drapeau doit REMONTER jusqu'a l'ecran, pas seulement exister en base** | 5 points cables, chacun verifie a l'application : `is_demo` ajoute au `select` de `get-client.ts` ET aux DEUX `select` de `get-clients.ts` (le principal et celui de la ligne 93, qui aurait ete un angle mort), plus les 2 mappings vers `isDemo`. Sans ces 5, la colonne existait et l'ecran restait vide | OK |
| T-026 | **Les DEUX surfaces marquees, pas une seule** | Liste (`client-list.tsx`, badge « Demo » colle au nom de l'entreprise, dans la colonne qu'on lit en parcourant) ET fiche (`client-header.tsx`, « Client de demonstration » place EN PREMIER, avant le type et le statut). Marquer une seule des deux aurait laisse une surface ou un tiers peut se tromper — c'est exactement le piege deja paye sur les actions de la fiche client | OK |
| T-026 | **Type optionnel, traite comme « pas demo » si absent** | `isDemo: z.boolean().optional()` sur `Client` et `ClientListItem`, `is_demo?: boolean` sur `ClientDB`. Un appelant qui ne selectionne pas la colonne n'explose pas, et l'affichage n'ajoute rien : **il vaut mieux oublier un badge que d'en poser un sur un vrai client** | OK |
| T-026 | **Rien de casse** | `npx vitest run packages/modules/crm` -> **953 tests passes, 119 fichiers**. `npx turbo build --filter=@monprojetpro/hub` -> **1 successful**, « Compiled successfully in 57s » | OK |
| T-026 | **Marquage visible par MiKL sur la prod** | NON VERIFIE — demande une session Hub authentifiee. A confirmer par MiKL | non verifie |
| T-026 | **Espace client (Lab/One) vu en impersonation** | ABSENT, et assume : si MiKL montre la VUE CLIENT d'un client fictif a un evaluateur, rien ne signale qu'elle est fictive. Perimetre non demande, signale plutot qu'invente. A ouvrir en T-026a si MiKL en a besoin pour le CII | absent |
| T-025 | **URL Pennylane unifiee dans `billing-sync`** | `PENNYLANE_BASE_URL` lit desormais `PENNYLANE_API_URL` avec repli sur la production, comme les 2 autres chemins d'acces. ⚠️ **CODE SUR DISQUE, EDGE FUNCTION NON REDEPLOYEE** — c'est donc l'ancienne version qui tourne. Sans consequence : MiKL a confirme le 20-09 que la variable vaut l'URL de production, soit exactement l'ancienne valeur en dur. Non deploye volontairement : le gain est nul aujourd'hui et un redeploiement mal maitrise casserait une synchro qui fonctionne (rappel `patch-log` : le CLI remet `verify_jwt=true` en silence) | partiel |
| T-025 | 🔎 **RETRACTATION du 2026-09-20 — le webhook « facture payee » de Pennylane N'EXISTE PAS, et rien n'est bloque** | MiKL ne trouvait pas l'ecran de configuration. Cause racine : il n'y en a pas. Documentation Pennylane : les webhooks sont **en beta fermee**, ne couvrent aujourd'hui que la creation de fichiers DMS et le statut e-invoicing, et leur page dit explicitement de **« se rabattre sur les endpoints de changelog »** en attendant. Le changement de statut de facture est annonce « sur la roadmap ». ⛔ **La `prod-checklist.md` ecrivait pourtant `Creer un webhook sur l'evenement "facture payee" (invoice.paid ou equivalent)` — le « ou equivalent » trahit la supposition : la ligne a ete ecrite sans jamais ouvrir l'interface.** J'ai bati dessus une alerte 🔴 « BLOQUANT POUR FACTURER » sans la verifier non plus. **2e rétractation sur ce chantier** | OK |
| T-025 | **Ce qui rend l'absence de webhook sans consequence — verifie dans le code, pas suppose** | `billing-sync` (cron 5 min, deja actif) lit les changelogs et traite deja le passage a `paid` : `isPaidNow && wasPreviouslyOverdueOrUnpaid` -> `paidFromOverdue` (`:378-381`), et `shouldActivateLabAccess` -> `labInvoicesPaid` (`:385-386`). La suite (`:578-598`) **met `lab_paid = true`, pose `lab_paid_at` et `lab_amount`, notifie MiKL et notifie le client**. Autrement dit : le paiement d'un forfait Lab active l'acces tout seul, avec au pire 5 minutes de latence. Le webhook n'apportait que l'instantaneite | OK |
| T-025 | ⚠️ **LA condition reelle, et elle n'a rien a voir avec un secret** | La jointure d'activation est `.in('pennylane_customer_id', customerIds)` (`:570`). Si `clients.pennylane_customer_id` est vide, **rien ne se rattache et l'activation n'a pas lieu** — silencieusement (`if (!client) continue`, `:579`). Or cette colonne est NULL pour les 3 clients actuels. Elle est renseignee quand le devis est cree DEPUIS le Hub. **Consequence pratique pour le 1er vrai client : emettre le devis depuis le Hub, jamais directement dans Pennylane.** C'est le vrai point de vigilance du tunnel de facturation | OK |
| T-025 | **PURGE FAITE le 2026-09-17, comptage rendu par la requete elle-meme** | `delete` + `select` en une transaction -> **16 documents supprimes, 8 mouvements de credits supprimes, 1 vestige `quote` retire de `billing_sync_state`, 3 etats remis a zero**. Etat verifie APRES : `billing_sync` 0, `coaching_credit_ledger` 0, `billable_items` 0, `quote_metadata` 0, `billing_sync_state` 3 lignes fraiches. **Les 3 clients sont CONSERVES** (3 lignes), conformement a la decision de MiKL | OK |
| T-025 | **Une suppression doit etre reversible avant d'etre lancee** | `docs/archives/donnees-demo-compta-2026-09-17.sql` ecrit AVANT le `delete` : script de restauration complet, identifiants explicites (donc rejeu idempotent), en-tete expliquant l'origine des donnees et le piege du ledger. MiKL avait dit « on les refera si besoin » — refaire coute une commande au lieu d'une soiree | OK |
| T-025 | **Constat 4 ABANDONNE — il n'y a AUCUN defaut d'idempotence sur les credits coaching** | Le snapshot pris avant suppression porte la preuve que la simple lecture des dates ne donnait pas : la ligne du **31-07** a `created_by: "system"` et une note redigee en francais (« Credit mensuel — aout 2026 ») ; les seules lignes du VRAI cron sont celles a `created_by: "monthly-billing"`, soit **01-08 et 01-09 — une par mois, parfaitement reguliere**. Le « double accrual » etait la superposition d'une ligne de demonstration saisie a la main et du cron reel. ⛔ **3 constats sur 6 se seront finalement reveles faux (4, 5, 6). Le champ `created_by` etait dans la table depuis le debut : je ne l'avais pas regarde.** | OK |
| T-025 | **Pourquoi « ca marchait avant » — la question de MiKL, repondue par le depot** | `docs/prod-checklist.md` compte **57 cases non cochees et 0 cochee** : la checklist de mise en production existe depuis avril et n'a JAMAIS ete tenue. Sa ligne 37 dit mot pour mot `- [ ] PENNYLANE_WEBHOOK_SECRET — secret HMAC partage avec Pennylane`. Le webhook est ne le 2026-04-15 (`c55317f`, story 13.4) ; le secret n'a jamais ete pose. **Ce n'est donc pas une regression, c'est une etape de mise en prod jamais faite.** Et pour les 2 autres webhooks, les tests passaient PARCE QUE `if (!secret) return true` desactivait la verification : le test disait vrai — ca marchait, pour tout le monde. ⚠️ La checklist n'est pas fiable dans l'autre sens non plus : `PENNYLANE_API_TOKEN`, `PENNYLANE_API_URL` et `SUPABASE_SERVICE_ROLE_KEY` sont bien poses dans Vercel alors que leurs cases sont vides | OK |
| T-024 | **PREUVE EN PRODUCTION, apres deploiement — les 2 sondes du diagnostic rejouees** | `POST /api/webhooks/cal-com` -> **HTTP 404** (route supprimee, elle rendait 200). `POST /api/webhooks/contact-form` sans signature -> **HTTP 500 `{"error":"Server configuration error"}`** (elle rendait 400, c'est-a-dire qu'elle franchissait la verification). Les deux portes constatees ouvertes le 16-09 sont fermees, verifie sur `hub.monprojet-pro.com`, pas en local | OK |
| T-024 | **TROUVAILLE COLLATERALE — le webhook Pennylane ne fonctionne pas non plus** | J'avais interroge `/api/webhooks/pennylane/paid` comme simple TEMOIN, en attendant qu'il reponde normalement. Il rend **500**. Lecture du code (`route.ts:90-94`) : ce 500 ne peut signifier qu'une chose, `PENNYLANE_WEBHOOK_SECRET` est absent de Vercel. La route se comporte bien — elle refuse au lieu d'accepter — mais le canal est MORT. ⚠️ **Consequence directe sur la facturation reelle : quand Pennylane annoncera qu'une facture est payee, le Hub refusera la notification et MiKL ne saura pas que son client a paye.** Reporte en section 7. **Lecon : le temoin d'une sonde doit etre lu, pas suppose — je l'attendais vert, il etait rouge** | OK |
| T-024 | **Tour du 2026-09-17 — fermeture du 2e webhook, par suppression et non par secret** | Les lignes marquees 17-09 plus bas sont celles de ce tour ; celles du 16-09 portaient le durcissement de `contact-form`. NOTE DE METHODE : la gate OTTO a REFUSE le premier commit de ce tour, parce qu'elle lit la PREMIERE ligne de convergence portant l'identifiant et que je n'avais modifie que les dernieres. Elle avait raison — d'ou cette ligne en tete. **Une convergence se reecrit en TETE, sinon la gate ne voit pas le nouveau travail** | OK |
| T-024 | Le refus doit valoir en production sans casser le dev ni les tests | `lib/webhook-auth.ts` — fonction pure `decideWebhookAuth(secret, nodeEnv, nom)` rendant `verify` / `allow` / `misconfigured`. Secret pose -> on verifie TOUJOURS (meme hors prod) ; secret absent + `NODE_ENV === 'production'` -> refus 500 ; secret absent ailleurs -> passage. `npx vitest run apps/hub/lib/webhook-auth.test.ts` -> **8 tests passes** | OK |
| T-024 | Une variable declaree mais VIDE ne doit pas passer pour un secret | Cas le plus trompeur : elle apparait dans le tableau de bord Vercel, donc on la croit posee. `if (secret)` traite la chaine vide comme absente, verrouille par un test dedie | OK |
| T-024 | Ne pas confondre un `NODE_ENV` voisin avec la production | Comparaison stricte `=== 'production'`, verrouillee par un test sur `'preproduction'` — un `includes`/`startsWith` aurait ouvert ou ferme a tort | OK |
| T-024 | Les 2 routes branchees, plus aucun `if (!secret) return true` | `grep` sur les 2 fichiers : la ligne a disparu des deux. `verifyToken` et `verifySignature` rendent desormais un objet `{ ok, status, error }` au lieu d'un booleen — le booleen ne pouvait pas porter la distinction 401 (signature invalide) / 500 (configuration absente) | OK |
| T-024 | Rien de casse dans les webhooks existants | `npx vitest run apps/hub/lib/webhook-auth.test.ts apps/hub/app/api/webhooks/` -> **22 tests passes, 3 fichiers** (dont les 8 du webhook Pennylane, non touche). `npx turbo build --filter=@monprojetpro/hub` -> **1 successful** | OK |
| T-024 | Import resolvable par Vitest ET par Next | Premier essai en `@/lib/...` : **echec**, `Failed to resolve import` — l'alias n'est configure ni dans `vitest.config.ts` ni utilise ailleurs dans le Hub. Repasse en chemin relatif, tests verts. L'echec est garde ici : c'est la preuve que le test a reellement tourne | OK |
| T-024 | Quel recepteur Cal.com est reellement appele ? | TRANCHE PAR LA DONNEE, apres que DEUX documents du projet se soient contredits (`patch-log.md` disait l'Edge Function, `prod-checklist.md:132` disait la route Next). `calcom_bookings` — **seule** table ecrite par la route Next — contient **0 ligne**. La route n'a donc JAMAIS recu un appel. Cal.com parle a l'Edge Function, qui ecrit dans `meetings` (6 lignes). ⛔ **Lecon : quand deux documents se contredisent, ne pas choisir le plus recent — aller regarder ce que la donnee prouve** | OK |
| T-024 | Le danger reel du deploiement, mesure et non suppose | J'avais annonce a MiKL que le correctif couperait le formulaire de contact et les reservations. **FAUX, et corrige aupres de lui** : `activity_logs` ne contient **aucun** `prospect_created` (le formulaire n'a jamais cree personne) et les 6 `meetings` sont des RDV de demonstration sans `calcom_booking_id`. Aucun canal vivant a couper. J'avais raisonne sur ce que le code POUVAIT faire, pas sur ce qu'il AVAIT fait | OK |
| T-024 | Fermer la route morte plutot que la securiser | `git rm apps/hub/app/api/webhooks/cal-com/route.ts`. Verifie apres suppression : plus aucune reference au fichier dans `apps/` ni `packages/`. Poser `CALCOM_WEBHOOK_SECRET` dans Vercel aurait verrouille une porte qui ne menait nulle part | OK |
| T-024 | L'ecran qui DICTE l'URL a Cal.com doit suivre | `calendar-settings.tsx` affichait `{origin}/api/webhooks/cal-com` — l'adresse morte. Remplace par `calcomWebhookUrl`, derive de `NEXT_PUBLIC_SUPABASE_URL` (jamais en dur : une adresse figee enverrait les reservations d'une preview vers la base de production). **Sans cette ligne, supprimer la route aurait produit un ecran qui dicte une adresse morte** | OK |
| T-024 | La documentation qui dictait la meme mauvaise URL | `docs/prod-checklist.md:132` corrige, + une ligne ajoutee sur le secret a verifier cote Edge Functions et non Vercel | OK |
| T-024 | Rien de casse par la suppression | `npx vitest run` (webhook-auth + les 3 webhooks restants + `apps/client/middleware.test.ts`, qui cite la route dans ses cas) -> **100 tests passes, 4 fichiers**. `npx turbo build --filter=@monprojetpro/hub` -> **1 successful** | OK |
| T-024 | Les RDV Cal.com s'affichent-ils dans l'agenda du Hub ? | ❌ **NON — bug PREEXISTANT decouvert par l'inspection des consumers, pas cause par la suppression.** `agenda-page.tsx:169` appelle `getCalcomBookings`, qui lit `calcom_bookings` — table que seule la route morte alimentait. L'Edge Function, elle, ecrit dans `meetings`. Les deux ne se sont jamais parle. Reste-a-faire cree en **T-024a**, non absorbe en silence | absent |
| T-024 | Secret `CALCOM_WEBHOOK_SECRET` cote Edge Functions | CONFIRME PAR MiKL le 2026-09-17 : « oui calcom webhook secret est bien la ». Le vrai recepteur est donc protege | OK |
| T-024 | Secret `CONTACT_FORM_WEBHOOK_SECRET` | NON FAIT, et **volontairement reporte**. Ce n'est pas un reglage : le site vitrine — un AUTRE depot — doit calculer et envoyer l'en-tete `x-contact-token` (HMAC du corps). Le poser cote Hub seul couperait l'entree au lieu de la proteger. Le formulaire n'ayant jamais cree un seul prospect, le correctif le met en securite en attendant | non verifie |
| T-024 | Preuve que la prod refuse desormais | NON VERIFIABLE AVANT DEPLOIEMENT. Sondes a rejouer : `POST /api/webhooks/cal-com` -> **404** (route supprimee) et `POST /api/webhooks/contact-form` sans signature -> **500** (secret absent en production, refus au lieu d'acceptation) | non verifie |

> 115 lignes de convergence d'items clos archivees le 2026-09-20 dans `00-product-board-archive.md` (section 4ter). Aucune n'est perdue. Ne restent ici que les items du chantier en cours.

**Regles :**

- **Une ligne par case cochee** au tableau KIT COMPLET, ou par element de l'item du board.
- **Verdict** : `OK` · `partiel` · `absent`.
- **Verifier par la preuve, jamais de memoire** — un grep, un fichier ouvert, un test passe.
  Ecrire « livre » parce qu'on se souvient l'avoir code est exactement l'erreur que ce
  controle existe pour attraper.
- **Partiel ou absent -> ca ne commite pas.** Deux issues, jamais trois : finir maintenant, ou
  creer le reste-a-faire au board (`B-012` -> `B-012a`, avec son perimetre) et le dire a MiKL.
- **Le reste-a-faire porte le numero d'origine** : la filiation doit rester lisible six mois plus tard.
- Ne pas refaire une ligne a l'identique d'un commit a l'autre : le harnais le detecte et refuse.

> Cette section s'archive avec le « Livre » — c'est du passe verifie, reconstituable.

---

## 4. Livre

| ID | Titre | Version | Date reelle | Perimetre | Notes client |
|---|---|---|---|---|---|
| E-01 | Fondations monorepo — auth, RLS multi-tenant, sessions, design system | Epics 1-2 | 2026-02-10 -> 2026-02-17 | Interne | Setup Turborepo, auth client + MiKL (2FA), isolation RLS |
| E-04 | Validation Hub — file de demandes, decisions, workflows post-decision | Epic 7 | 2026-02-26 | A qualifier | Coeur du pilotage MiKL des soumissions Lab/One |
| E-05 | Elio — assistant IA multi-agents (Lab, Hub, One, One+) | Epics 6, 8, 14 | 2026-02-27 -> 2026-08-19 | A qualifier | Chantier le plus etendu de tout le projet |
| E-06 | Cycle de vie client — graduation Lab->One, suspension, cloture, RGPD | Epic 9 | 2026-03-04 -> 2026-03-05 | A qualifier | Migration Lab vers One, export RGPD, anonymisation |
| E-08 | Facturation — integration Pennylane (devis, abonnements, sync) | Epic 11 | 2026-03-07 -> 2026-04-15 | A qualifier | Polling Pennylane -> `billing_sync` -> Realtime |
| — | Rebrand Foxeo/Foxio -> MonprojetPro (domaine monprojet-pro.com) | — | 2026-04-14 | Interne | URLs, docs, marque |
| — | Refonte Vision v2 du One — socle Relation/Cockpits, agent FORGE, offres ①②③ | — | 2026-06-24, prix ajuste 2026-07-31 | Interne | Redefinit toute la strategie produit |
| — | Entree de connexion unique + bascule de domaine prod + sessions Hub bornees | — | 2026-08-04 | Interne | Durcissement securite + UX |
| — | Elio One devient l'intermediaire de MiKL (relais chat + prise de nouvelles) | — | 2026-08-19 | A qualifier | Dernier grand chantier avant ce board |
| — | Ruflo raccorde au MCP (scope User) — pipeline qualite code operationnel | — | 2026-08-19 | Interne | 3 mois sans controle qualite avant ce raccordement |
| T-023 | Pieces jointes MenuFacile visibles dans le cockpit (vignettes, visionneuse, PDF, renouvellement d'URL, badge trombone) | — | 2026-09-15 | Interne | **Confirme de visu par MiKL** : les 2 captures de Lucie s'affichent. Sens inverse (repondre avec un fichier) -> T-023a |
| T-023a | Repondre AVEC des pieces jointes depuis le Hub — selecteur, compression, depot direct vers le Storage MenuFacile, regle tout-ou-rien | — | 2026-09-16 | Interne | **Confirme par MiKL sur la prod** : « ca marche au top ». Les 2 sens sont desormais couverts. 3 briques remontees en bibliotheque au passage (`attachment-constraints`, `compress-image` -> `@monprojetpro/utils` ; `AttachmentsPicker` -> `@monprojetpro/ui`) |

> 5 items supp. (E-02, E-03, E-07, E-09, E-10) archives dans `00-product-board-archive.md` (§4). Aucun ID perdu.

---

## 5. Ecarte et gele — registre des decisions

| ID | Titre | Decision | Raison | Decide par | Date | Reouvrable ? |
|---|---|---|---|---|---|---|
| F-007 | Kit de sortie One — instance dediee (Vercel + Supabase + repo transferes au client) | Ecarte | Utopie technique — tables `client_instances`/`instance_transfers` a 0 ligne, jamais declenche | MiKL | 2026-06-24 | Non |
| F-009 | Elio One+ agentique (generation de documents, actions modules) pour l'offre premium | Ecarte / corrige | Contradiction avec le positionnement One+ = coaching humain ; restait actif en base pour 2 clients reels | MiKL | 2026-08-19 | Oui — au devis, jamais inclus dans un tier |
| — | Prix de l'offre One | Change | 39€/mois -> 49€/mois | MiKL | 2026-07-31 | — |

> 3 items supp. (F-008, offre ① standalone, kit de sortie LAB garde) archives dans `00-product-board-archive.md` (§5).

---

## 6. Hors devis a chiffrer

| ID | Titre | Demande par | Origine (appel / mail / reunion) | Date demande | Estimation | Statut chiffrage |
|---|---|---|---|---|---|---|
| | | | | | | |

> Aucune trace de demande hors devis — produit interne MPP, pas un chantier facture. A confirmer MiKL.

---

## 7. En attente d'action externe

| Quoi | Qui doit agir | Bloque quel item | Depuis |
|---|---|---|---|
| Fournir l'adresse e-mail de reference du comptable **et** un exemple d'e-mail Pennylane (pour calibrer la reconnaissance) | MiKL | F-010 | 2026-08-24 |
| Autoriser le rejeu de la migration 00094 en production (mise en coherence, sans effet visible) | MiKL | T-012 | 2026-08-24 |
| ~~Poser `PENNYLANE_WEBHOOK_SECRET`~~ — **RETRACTE le 2026-09-20, ce n'etait PAS bloquant.** Rien a faire cote MiKL. Raison : l'evenement « facture payee » **n'existe pas** chez Pennylane (webhooks en beta fermee, changement de statut de facture annonce « sur la roadmap »), et leur propre documentation dit de se rabattre sur les **endpoints de changelog** — ce que `billing-sync` fait deja toutes les 5 minutes. Detail et preuves au journal | — | — | clos 2026-09-20 |
| Poser `CONTACT_FORM_WEBHOOK_SECRET` — **PAS un simple reglage** : le site vitrine (autre depot) doit aussi calculer et envoyer l'en-tete `x-contact-token`. Reporte d'un commun accord, le formulaire n'ayant jamais cree un seul prospect. Depuis le 17-09 il refuse (500) au lieu d'etre ouvert | MiKL | T-024 | 2026-09-17 |
| Fournir `VERCEL_TOKEN` / `SUPABASE_MANAGEMENT_TOKEN` pour un kit de sortie complet (non urgent) | MiKL | F-007 si rouvert | 2026-07-03 |
| Backups Supabase actives (le volet « compte Pennylane prod actif » de cette ligne est CLOS — voir journal 16-09) | MiKL | Onboarding client | 2026-04-15 |

> Les 3 suspects « traite mais jamais coche » ont ete TRANCHES PAR SONDE le 2026-09-16 : DNS/SSL **fait** (retire) · `env-probe-temp` **supprimee** (retire) · T-003 **PAS fait**, requalifie en point de securite actif (T-024). Detail et methode au journal.

---

## 8. Journal des decisions et changements de perimetre

| Date | Evenement | Items concernes | Decide par |
|---|---|---|---|
| 2026-09-16 | **T-023a CLOS — MiKL confirme sur la prod : « ca marche au top ».** Les deux sens des pieces jointes MenuFacile sont couverts : recevoir (T-023, clos le 15-09) et repondre avec (T-023a). Avant de faire tester MiKL, le cablage de l'ecran a ete verifie par lecture ligne a ligne plutot que suppose depuis les tests unitaires — parce qu'une logique testee a 11 tests et un bouton qui ne l'appelle pas produisent exactement le meme vert. Le harnais a refuse l'acces au `.env.local` pour une sonde de bout en bout (regle secrets), et c'etait la bonne issue : meme avec le secret, la sonde n'aurait prouve que le depot, jamais que la destinataire voit la capture dans son app. **Leçon : quand la derniere maille est derriere une session humaine, ecrire le protocole de test en etapes numerotees pour MiKL vaut mieux que chercher un contournement technique** | T-023a, T-023 | MiKL |
| 2026-09-16 | **Les 3 « suspects traites mais jamais coches » tranches par sonde — 2 etaient faits, 1 etait FAUX et cachait une faille.** (1) DNS/SSL : `curl` sur les 4 domaines -> `hub.` 307 vers `app./login`, `app.` 307 vers son login, apex 308 vers `www.`, `www.` 200, **SSL valide partout** (`ssl_verify=0`), IP Vercel. FAIT. (2) `env-probe-temp` : `list_edge_functions` -> 15 fonctions, elle n'y est PAS. SUPPRIMEE. (3) T-003 : **PAS fait du tout.** Sonde comportementale concue pour n'ecrire aucune donnee (la verification de signature precede la validation du corps, donc 401 = secret pose / 400 = secret absent) -> `contact-form` rend **400 « name and email are required »** et `cal-com` rend **200 `{received:true}`** : dans les deux cas la signature a ete franchie, **les secrets ne sont pas poses en production**. ⛔ **LECON, et elle vaut pour les 3 autres suspects du meme genre : un soupcon « c'est surement deja fait » avait une chance sur trois d'etre faux, et c'est precisement celui qui portait une faille de securite.** Ne jamais retirer une ligne d'attente sur une impression — la sonde coute 30 secondes | T-003, T-010, T-024 | MAX |
| 2026-09-16 | **Le secret `PENNYLANE_API_TOKEN` des Edge Functions EXISTE et fonctionne — ligne d'attente ouverte depuis le 03-07 close par preuve.** Lecture de `billing_sync_state` : `last_sync_at` au 16-09 20:30 avec `consecutive_errors: 0` pour `customer`, `invoice` et `subscription`. Or `billing-sync/index.ts:916` refuse de demarrer sans le token (500), et `last_sync_at` + `consecutive_errors: 0` ne sont ecrits qu'APRES un appel reussi (`:844-850`) — un etat frais ne peut donc pas etre un faux positif. Comme l'URL y est **en dur sur `app.pennylane.com`** (`:24`), cela prouve aussi que le compte interroge est le compte de PRODUCTION, pas un bac a sable | Compta, T-025 | MAX |
| 2026-09-16 | **FORGE T-021 CLOSE : rien a extraire du correctif chat/temps reel, MiKL confirme.** Question ouverte depuis le 09-09. Avant de clore, l'affirmation du board a ete VERIFIEE dans le catalogue plutot que recopiee (`grep` sur `~/.claude/catalogue-modules-mpp.md`) : les deux kits existent bien en etat **Extrait**, et `fil-de-discussion-complet` couvre nommement le badge de fraicheur ET la detection d'abonnement mort — « mise a jour interrompue », la panne muette. Ce qui a ete ecrit sur T-021 est donc l'application locale de `realtime-refresh-supabase-next` + `fil-de-discussion-complet`, pas une brique nouvelle. Rien a faire, aucun 9e kit. **Leçon : une question FORGE se clot en ouvrant le catalogue, jamais en relisant la ligne du board qui la pose** — c'est exactement ce reflexe qui, le 09-09 sur `auth-supabase-next`, avait revele que le kit couvrait le sujet mais le couvrait MAL | T-021 | MiKL |
| 2026-09-16 | **FORGE : `upload-operator-attachments.ts` N'EST PAS extrait en kit — on attend une 2e occurrence.** Question posee le 15-09, restee sans reponse explicite ; hypothese par defaut annoncee a MiKL et appliquee au silence. Le raisonnement : ce n'est pas le pattern deja extrait dans `pieces-jointes-supabase`. Ici on depose chez un TIERS via une URL pre-signee que SON API nous rend, avec une regle metier « tout ou rien » (un fichier echoue = le message entier ne part pas). **1re occurrence chez MPP** — or les trois briques extraites le 15-09 l'ont ete apres 3 occurrences reelles, pas apres une. Extraire maintenant figerait une abstraction calquee sur un seul fournisseur, alors que la forme varie beaucoup d'une API a l'autre (fichier en direct, systeme de tickets, callback de confirmation). Item CANDIDAT au catalogue, pas abandonne : a rouvrir des qu'une 2e integration tierce demande le meme geste | T-023a | MAX (hypothese par defaut, silence de MiKL) |
| 2026-09-15 | **T-023a debloque le jour meme : MenuFacile a livre les 2 endpoints.** Notre spec a ete suivie, avec 4 ecarts a connaitre AVANT de coder : un `token` supplementaire dans la reponse (pour `uploadToSignedUrl`), `expires_in` a 7200 et non 600, extension imposee par le `mime_type` declare, et plafond REEL a 10 Mo et non 5 — leur controle de taille est declaratif, c'est a nous d'appliquer 5 Mo avant l'upload. Sur le piege Storage, ils ont trouve le fait qui manquait a notre analyse comme a la leur : `get_my_contact_thread` filtre sur `c.user_id = auth.uid()`, donc un fil n'est lisible que par SON AUTEUR, jamais par tout son foyer. Le destinataire est deja proprietaire du dossier — notre option 2 (prefixe `admin/` + policy elargie) aurait ajoute de la surface de securite pour rien. Lecon pour nos prochaines specs : proposer des options est utile, mais celle qu'il faut retenir peut etre tranchee par un detail du modele que seul le depot d'en face peut lire. Ne jamais presenter deux options comme equivalentes sans avoir vu ce detail | T-023a, T-023 | MAX |
| 2026-09-15 | **T-023 CLOS — MiKL voit les 2 captures de Lucie dans son cockpit.** Fin d'un bug ouvert le 10-09 : une cliente joignait des captures d'ecran que personne ne voyait jamais. Ce que l'affaire aura coute en detours, et pourquoi : pendant 5 jours la question a ete « qui ne fait pas son travail, le guichet ou le Hub ? », chacun des deux depots supposant que le probleme etait chez l'autre. La reponse tenait en un `grep attachment` cote Hub (zero occurrence) — 30 secondes de verification contre 5 jours d'allers-retours. **Quand deux systemes se renvoient la balle, aller lire le code du consommateur avant de discuter du contrat de l'emetteur** | T-023 | MiKL |
| 2026-09-15 | **T-023 en production.** Rien n'a eu besoin d'etre lance : contrairement a MenuFacile (deploiement Vercel manuel), le Hub est connecte a Git — `apps/hub/vercel.json` porte `ignoreCommand: npx turbo-ignore`, donc chaque push sur `master` declenche un build, saute si l'app n'est pas concernee. Les deux commits du jour ont bien produit un deploiement « Production – monprojetpro-hub » en **success** (verifie par l'API GitHub, pas suppose). A retenir : **ne pas transposer la procedure de deploiement d'un projet MPP a un autre** — ici, proposer « je deploie » etait deja un contresens, le deploiement etait fait depuis le push | T-023 | MAX |
| 2026-09-15 | **T-023a livre le soir meme : le Hub peut repondre AVEC des fichiers.** MenuFacile livre F-030c (guichet v16) ; verifie par sonde avant de coder (26 endpoints au lieu de 24, les 2 routes presentes) plutot que sur parole. Trois constats qui ont change le plan : (1) les regles du Hub collaient DEJA exactement a celles du guichet (10 Mo, 3 fichiers, 7 types) — la limite de 5 Mo que j'avais proposee dans la spec aurait refuse a tort des fichiers acceptes par le bucket, elle est abandonnee ; (2) les 3 briques necessaires existaient dans le module `support` — remontees dans `@monprojetpro/utils` et `@monprojetpro/ui` par `git mv` plutot que dupliquees, parce qu'un module ne peut pas en importer un autre ; (3) l'affichage des pieces envoyees par l'operateur n'a demande AUCUN code : `ContactAttachments` etait deja rendu sur chaque bulle sans tester l'expediteur. Regle « tout ou rien » a l'envoi : si un fichier echoue, le message ne part pas — un message annoncant une capture absente est pire que pas de message. Limite assumee : un fichier deja depose avant un echec reste dans le Storage (invisible, mais le guichet n'expose aucune route pour le retirer) | T-023a | MAX |
| 2026-09-15 | **MiKL dit OUI a FORGE : le volet RECEPTION des pieces jointes rejoint la bibliotheque.** Question posee car le pattern revenait pour la 3e fois (GuardVeto, Lab foxeo-one via T-017, MenuFacile). Choix retenu : ENRICHIR le kit `pieces-jointes-supabase` existant plutot que creer un 8e kit — il couvrait l'envoi, il couvre desormais les deux bouts. Ajoute : `lib/signed-attachment.ts` (3 helpers purs), `lib/signed-attachment.test.ts` (11 tests EXECUTES HORS du projet d'origine -> 11 passed), `components/signed-attachments.tsx` (neutralise : l'API tierce n'entre plus que par la prop `onRefreshUrl`, les couleurs par `accentClassName`, les libelles surchargables), CHANGELOG cree. Inscrit au catalogue et complete dans `ui-patterns-kits.md` — la verification de completude a revele que le pattern « Upload de fichier » ne decrivait QUE l'envoi : la reception (URL signee qui expire, renouvellement, type MIME de repli, pop-up bloque) n'y figurait nulle part. L'extraction a rendu le code plus strict que l'original : `url_expires_at` et `size_bytes` deviennent optionnels, echeance absente = non expiree | T-023, T-023a | MiKL |
| 2026-09-15 | **T-023a : blocage confirme par sonde, spec ecrite pour MenuFacile.** MiKL demande de finir le chantier (2e sens : repondre AVEC des fichiers depuis le Hub). Avant de coder, le blocage annonce par le board a ete RE-VERIFIE sur le guichet reel — precaution prise justement parce que l'inverse venait de se produire sur T-023 (board en retard sur un travail deja livre). Resultat : blocage bien reel, le guichet v3 n'expose aucune route d'upload. Livrable du tour : `spec-attachments-operateur.md`, qui donne a MenuFacile les 2 endpoints a ecrire, le contrat de lecture a preserver, le piege des policies Storage, et le protocole de validation. Decision assumee : ZERO ligne de code cote Hub — deviner les noms de champs produirait un ecran qui compile et n'envoie rien. Trouvaille au passage : la route de renouvellement d'URL utilisee en production repond bien (HTTP 200) mais ne figure pas dans l'index `GET /` du guichet | T-023a | MAX |
| 2026-09-15 | **T-023 PROUVE par sonde reelle, apres feu vert de MiKL.** Le doute portait sur la seule chose qu'on ne peut pas deduire du code : ce que le guichet renvoie vraiment. Resultat, en 6 points : `attachment_count` present sur la liste (2 sur le fil de Lucie, 4 sur un autre) · les pieces decrites avec tous les champs obligatoires + `size_bytes` · **le fichier se telecharge pour de vrai (HTTP 200, `image/webp`, `content-length` = exactement le `size_bytes` annonce)** · la route de renouvellement rend bien une URL fraiche · un identifiant FABRIQUE est refuse (HTTP 400, controle d'appartenance au fil). La sonde a aussi corrige une erreur de ma part : mon test affirmait que le guichet ecrit ses dates en `+00:00` alors qu'il ecrit `…Z` — commentaire faux rectifie, les deux notations couvertes. Trouvaille collaterale a surveiller : `MENUFACILE_ADMIN_API_URL` est ABSENTE du `.env.local` (la sonde utilisait une URL en dur). En production elle existe forcement, sinon le Hub n'afficherait aucun message de contact — mais ce raisonnement n'est PAS une verification, d'ou la ligne en section 7 | T-023 | MiKL + MAX |

> 34 entrees de journal anterieures archivees le 2026-09-20 dans `00-product-board-archive.md` (section 8bis).

---

## Conventions

Voir le template MPP `installation WF base/templates-projet/docs/00-product-board.md`.

---

*OTTO. Board vivant — toute nouvelle idee entre par la section 2 (INBOX).*
